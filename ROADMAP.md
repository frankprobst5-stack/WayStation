# WayStation — Roadmap

**Online for awareness. Offline for operations. Built to shine when the grid goes down.**

WayStation is an offline-first communications and field-operations platform for amateur radio, emergency communications, preparedness, and resilient local coordination. It is not a collection of ham-radio panels. Its operational loop is:

**OBSERVE → UNDERSTAND → DECIDE → COMMUNICATE → DOCUMENT → SYNCHRONIZE**

Online connectivity enriches the local operational picture. When connectivity degrades, WayStation retains cached information with honest age and provenance. When the grid fails, live RF, local hardware, nearby peers, and locally stored operational data take over. Grid-down mode is not a crippled online dashboard — it is the environment WayStation exists to serve.

WayStation is the comms component of a larger project, [Citadel](https://github.com/frankprobst5-stack/Project-Citadel) — a self-hosted, offline-first home command center. Citadel launches WayStation directly (see `app/src-tauri/src/lib.rs`'s deep-link registration); the two are separate applications, not one embedded in the other. The eventual deployment target is **8 independent homes**, each running its own Citadel+WayStation instance, coordinating with each other over long-range RF when normal internet/phone service is down.

**Deployment goal, refined 2026-09-03:** Citadel integration remains the primary target — that doesn't change. WayStation is also intended to eventually work standalone for the wider public, where an operator without Citadel plugs in their own local AI/map server/whatever else they need instead. This mostly falls out of the architecture already in place rather than requiring a separate build: every Citadel-dependent feature (map tiles, and the weather/AI/power items in **Planned backlog** below) follows the same configurable-host-with-graceful-degrade pattern already proven by the map tiles' existing OpenFreeMap fallback. The hard-requires-Citadel audit is done (see Phase F below for the real evidence) and README.md now has real "no-Citadel setup" documentation.

**Non-negotiable principle:** every future feature must either strengthen the shared operational core, provide it with trustworthy data, deliver its objects, help the operator act on them, or improve field reliability. New panels must not become isolated islands.

**Second non-negotiable, added 2026-09-03:** discovering a peer is never the same as syncing with one. Any future peer-discovery mechanism (mDNS, etc.) may only ever reveal that a station exists on the network — moving actual data to it is always a deliberate, explicit operator choice, made every time, no exceptions for family or friends. This matters today and matters more once WayStation is public: a stranger's instance showing up at a multi-org exercise or on a shared network must never be able to pull incident/personnel data just by being discoverable.

---

## How progress is classified

| Status | Meaning | Release claim |
|---|---|---|
| **BUILT + VERIFIED** | Implemented and tested against real software, live network services, local processes, database tests, or reproducible simulations. | Working within the tested environment. |
| **BUILT — HARDWARE VALIDATION PENDING** | Code path exists, but real RF, noise, range, timing, multi-node behavior, or equipment interoperability hasn't been proven. | Experimental; not field-proven. |
| **PARTIAL** | A useful portion exists, but reception, sending, acknowledgement, synchronization, UI, or error handling remains incomplete. | Limited capability only. |
| **PLANNED** | Agreed scope with a known place in the architecture, but no implementation. | Not available. |
| **DISCOVERY** | A real possibility requiring research, protocol decisions, safety analysis, or hardware access. | No commitment until investigated. |

Frank owns no dedicated mesh/AREDN/SDR test hardware yet — the app is being built to be software-complete (simulation, replay, and real local-service integration where no radio is required) before hardware becomes the bottleneck. Testers with real equipment are WayStation's distributed hardware laboratory; their job is to validate reality and report evidence, not to finish designing the application.

---

## Built and verified

**Foundation**
- Tauri desktop shell + window management, SQLite schema with `source`/`fetched_at`/`via` provenance conventions, connectivity state machine (online/degraded/RF-only/manual-offline), panel plugin architecture with category-based registration.
- Migration system: 30 migrations, each atomic (own transaction, `user_version` only advances on success — found necessary the hard way after a real mid-migration process kill on 2026-08-30 left an earlier version of the schema half-applied). Full chain, idempotency, and partial-failure-leaves-no-trace are all covered by real tests (`db.rs`'s `#[cfg(test)] mod tests`).
- Offline world map (day/night terminator, greyline), station identity + local/Zulu time bar, app-wide staleness/freshness display convention, database backup/export (atomic `VACUUM INTO`), Diagnostics panel.

**EmComm core**
- NWS active alerts (severity-colored, cached honestly on fetch failure), net control roster (check-in/out, last-heard, traffic count), ICS-213 message form, ICS-309 log + export, resource tracking, "Prepare for Offline" forced-poll check.

**Operating awareness**
- Space weather, band-condition table, DX cluster live feed, POTA spots, Hamlib CAT/rotator control, ADIF QSO log, satellite pass prediction with Doppler, bearing/distance and RF calculators, PSKReporter reception reports, repeater lookup, contest calendar, band plan reference.

**Flight tracking (ADS-B) — online half built and verified, 2026-09-04**
- `flight_tracking.rs`: polls OpenSky Network's free, keyless public API every 5 minutes in a bounding box (~135mi half-width) around the station's own grid square — verified live before writing any code (a real `curl` against the endpoint, same "check for a ToS block before building" discipline `pota.rs` already established; none found). Migration v40: `aircraft_tracks`, same replace-on-successful-fetch pattern as `pota_spots`/`psk_spots` — a live snapshot of who's in the air right now, not an accumulating history. `FlightTrackingPanel.tsx` (EmComm tab) lists nearby aircraft with callsign, altitude, speed, heading, on-ground status, and honest per-fetch staleness.
- OpenSky's state-vector JSON is a real third-party format (positional array, mixed types, fields legitimately null) — parsed defensively by index into `serde_json::Value` rather than a typed tuple, so one unexpected field doesn't fail the whole record. Falls back to geometric altitude when barometric is null (a real, observed case for grounded/transitioning aircraft), trims OpenSky's space-padded callsigns.
- **This is deliberately the online half only.** The local SDR fallback (`dump1090`/`readsb`, decoding real 1090MHz ADS-B directly when the internet is down) is real remaining work, honestly not built — genuinely blocked on SDR hardware the same way multi-node mesh testing is blocked on a second physical node, not on more code. Named explicitly here and in `flight_tracking.rs`'s own module doc comment rather than silently absent. No grid square configured means no bounding box to query — the panel and source-health both say so plainly rather than showing stale or wrong data.
- Verified: 4 new tests (93 total, 89 passing, 4 live-only skipped) covering real OpenSky field-order parsing, the geo-altitude fallback, callsign trimming, and rejecting a state vector with no aircraft identity at all. Clean TypeScript build, clean release build (`localhost:1420` count still 1). **Confirmed working live by Frank, 2026-09-04** — real OpenSky data rendering correctly in the actual running app.
- **Tactical Map integration, 2026-09-04** — `TacticalMapPanel.tsx` now plots every currently-tracked aircraft with a known position as a white pin (callsign, altitude, speed, on-ground status), same "Aircraft (ADS-B)" legend entry. Not filtered by the selected incident, same reasoning as mesh nodes/net check-ins/the generic resources board — the whole point of tracking flights toward a disaster area is spotting something relevant *before* anyone's necessarily declared an incident around it yet, so gating this behind an incident filter would work against the reason it exists. Requested directly by Frank after confirming the standalone panel worked: "we should wire it in to the map.. it would be easyer to track flights to disaster areas."

**Mesh (Meshtastic)**
- Real TCP client verified against `meshtasticd`'s actual wire protocol — and, as of 2026-09-03, verified live against a real running `meshtasticd` instance for the first time in an automated, repeatable test (`mesh.rs`'s `#[ignore]`d live tests), not just by hand months earlier. Node list, channel chat with real delivery status (sent/delivered/failed — a failed send says why, never silently claims success). Direct messages, position send/request, and synchronized pins are built but still genuinely unverified for want of a second node — tried running two separate `meshtasticd --sim` instances on one host to prove real multi-node delivery without hardware; they turned out to be fully RF-isolated from each other by design (no shared virtual medium between them), so that specific gap is still open and still blocked on a second physical node or a proper multi-node simulator, not on more code.

**Off-grid messaging**
- Pat/Winlink process orchestration (auto-launch, status, inbox, orphaned-process auto-reclaim) and JS8Call TCP client (status + inbox) both working.

**Citadel integration — built and verified end-to-end, 2026-09-01**
- WayStation registers a `waystation://` custom URI scheme (`tauri-plugin-deep-link`) so Citadel's cockpit can launch it directly. `tauri-plugin-single-instance` runs first in the plugin chain — a second launch attempt while WayStation is already running focuses the existing window instead of spawning a competing process (which would otherwise fight over the same SQLite file and the same mesh/rig/rotator TCP ports).
- Citadel's "Communications Hub" tile (`appdata/cockpit/index.html`) now opens `waystation://open` instead of the old `comms.html`.
- Verified for real, both cold-start and already-running cases, from Citadel's live cockpit in an actual browser. One real lesson from getting here: a plain `cargo build --release` is **not** equivalent to a real production build — it still bakes in the Vite dev server URL and embeds none of the frontend. Only `tauri build` (or `npm run tauri build`) produces a binary that doesn't depend on a dev server being alive. Verify any future release binary with `strings target/release/waystation | grep localhost:1420` before trusting it.

**Canonical object header + Transport trait — built and verified, 2026-09-01**
- v30 migration: `uuid`, `revision`, `updated_at`, `incident_id`, `expires_at`, `trust_state` added to `messages` and `map_markers` — the shared header every object type needs before cross-station sync can exist. `uuid` is the stable cross-instance identity (local autoincrement `id` will collide the moment more than one of the 8 planned home instances exists); backfilled automatically for any existing rows on every `db::open()`.
- `Transport` trait (`transport.rs`) with full `MeshTransport`/`WinlinkTransport`/`Js8CallTransport` implementations, derived from three real, already-working send paths rather than designed speculatively. `dispatch.rs`'s two near-duplicate if-chains (one for messages, one for markers) collapsed into one shared loop. Deliberately preserves a real, pre-existing asymmetry: ICS-213 messages get per-transport-tailored formatting (mesh's compact single line vs. Winlink's full body vs. JS8Call's most-compact-of-all, matching real JS8 bandwidth limits), while situational markers share one exact wire format across all three transports, since a marker is a parseable protocol another station decodes, not prose.
- Verified at three levels: 18 automated tests (10 new, including exact-string tests against the pre-refactor wire formats — this caught a real mistake in the first draft that would have collapsed the three message formats into one); a clean release build; and two live integration tests run against Frank's actual logged-in Pat and JS8Call sessions, with the resulting test message independently confirmed sitting in the real Pat outbox via the API. **Mesh was not verified live in this slice** — no Meshtastic node was connected this session, so `MeshTransport` had unit-test coverage only. Closed 2026-09-03, see that changelog entry — the app-level `MeshTransport`/`dispatch.rs` path connecting through the real running daemon is still open, since these new tests exercise `mesh.rs`'s own wire functions directly, not the full Tauri-app-state-coupled send path.

**Durable delivery-attempt history — built and verified, 2026-09-01**
- v31 migration: `delivery_attempts` table, keyed by object `uuid` (not the local integer id). `dispatch.rs`'s `try_dispatch`/`try_dispatch_marker` now share one `dispatch_with_logging()` helper that records every transport attempt — success or failure — instead of only the winning one. Closes the real gap where `dispatch_status`/`dispatched_via` only ever showed the latest outcome, never the road there.
- Verified: 2 new tests proving a message tried on a failing transport then a succeeding one leaves both attempts queryable in order (22 tests total, 20 passing + 2 live-service-only skipped by default), clean release build, no new clippy issues.

**Two-instance sync proof — reconciliation logic built and verified, 2026-09-01**
- `sync.rs`: a manifest (`uuid`/`kind`/`revision`/`updated_at`/`content_hash`) is the lightweight thing two stations exchange first; `uuids_needed_from()` diffs two manifests to decide what to actually fetch — missing entirely, strictly newer revision, *or* same revision with a different content hash (a real potential conflict, not just "who's ahead"). `export_objects()` fetches full content only for what's actually needed. `merge_incoming()` applies it: insert if unknown, adopt if strictly newer, flag-and-change-nothing on a genuine conflict, ignore if local is already ahead. `trust_state` is forced to `'received'` on ingest regardless of what a peer's object claims about itself — a station's own database is the only thing allowed to call something `'local'`.
- Deliberately transport-agnostic: the reconciliation functions operate on `&Connection` and plain data with no assumption about how the bytes moved. File-based exchange (`export_manifest_to_file`/`manifest_diff_from_file`/`export_objects_to_file`/`import_objects_from_file`) is the first real transport, chosen because the roadmap explicitly names it and it proves the logic for real without WayStation needing to become a network server yet — a local TCP listener or eventually real mesh/AREDN can wrap the exact same functions later.
- `WAYSTATION_DATA_DIR` env var override on `db::data_dir()` lets two real WayStation instances run on one machine against two separate databases — the actual mechanism for proving "two stations" without needing two physical computers.
- Verified: 3 tests, each running the real exchange sequence (export manifest → diff → export requested objects → merge) between two genuinely separate SQLite databases — not a simplified stand-in. Proves new-object convergence in both directions, a stale instance correctly adopting a newer revision, interrupt-resume (re-running an already-converged exchange is a clean no-op), and a genuine conflict (two stations editing offline, unaware of each other, same revision) being flagged and left unresolved rather than silently overwritten. Clean release build.
- **UI added, 2026-09-01: `SyncPanel.tsx` (Settings → Peer Sync).** Two buttons — Export My Data to File, Import From File — plus a combined `export_full_bundle_to_file`/reused `import_objects_from_file` command pair that skip the selective-fetch optimization (a real thing for bandwidth-constrained RF later, not meaningful for a file exchange) and just export everything, letting `merge_incoming`'s already-safe no-op behavior sort out what's actually new. Shows insert/update/already-up-to-date counts and lists any conflicts by uuid without ever silently overwriting one. **Honest gap: the panel itself hasn't been clicked through by a human yet** — TypeScript compiles clean and the release build packages correctly, but exercising two real running instances via the actual UI needs Frank's hands-on pass, not something verifiable by an agent that can't drive a native GUI window.

**WSP/1 formalized — built and verified, 2026-09-02** — see [WSP-1.md](WSP-1.md) for the full spec. Every sync export is now a versioned `WspEnvelope` (`wsp_kind`/`wsp_version`/`origin_callsign`/`generated_at`) instead of a bare JSON array; a future format change or the wrong file kind now fails with a clear error instead of a confusing deserialize panic or silent misparse. 4 new tests (29 total, 27 passing + 2 live-only skipped).

**Phase D slice 1: incident lifecycle — built and verified, 2026-09-02**
- `incidents` table (v32 migration): real multi-incident create/active/closed lifecycle with the canonical uuid/revision/updated_at/trust_state header, separate from the pre-existing `incident_info` singleton (unchanged — still the quick-glance "current situation" card).
- `set_message_incident`/`set_marker_incident`: finally puts the `incident_id` column (present on messages/map_markers since v30, unused until now) to work — tags existing traffic against a declared incident, rejecting a dangling/unknown incident reference rather than writing one silently.
- `IncidentsPanel.tsx` (EmComm → Incidents): declare an incident, see active vs. closed, close one.
- Verified: 6 new tests (34 total, 32 passing + 2 live-only skipped) — create→close lifecycle with a real revision bump on close, closing an unknown incident failing clearly, active-before-closed sort order, tagging a real message and reading it back, clearing a tag, and a dangling-uuid tag attempt being rejected with the message left untouched. Clean release build.
- **Explicitly not this slice:** personnel/team tracking, expanded resource-request objects, structured SITREP, operational timeline, and the tactical map actually rendering incident objects instead of independent pins. Real remaining Phase D work, not silently folded into "done."

**Phase D slice 2: personnel tracking — built and verified, 2026-09-02**
- `personnel` table (v33 migration): a standing roster, separate from `net_roster` (which tracks radio check-in state, not operational assignment — the same person can appear in both without conflict, they mean different things). Status vocabulary: available/assigned/en_route/on_scene/unavailable/off_duty/emergency. `incident_id` optional and reassignable — personnel exist independent of any one incident, get assigned when one exists.
- `assign_person_to_incident`: same dangling-reference protection as message/marker tagging — an unknown incident uuid is rejected, not written.
- `PersonnelPanel.tsx` (EmComm → Personnel): add a person, change status, assign/clear incident from a live dropdown of active incidents.
- Verified: 6 new tests (40 total, 38 passing + 2 live-only skipped) — default state, rejecting an unrecognized status with the record left untouched, every documented status accepted, real incident assignment + clearing, rejecting assignment to an unknown incident, and sort order (actively-assigned personnel before everyone else, not just creation order). Clean release build.
- **Explicitly not this slice:** expanded resource-request objects, structured SITREP, operational timeline, tactical-map-by-incident. Same honesty rule as slice 1.

**Phase D slice 3: resource requests — built and verified, 2026-09-02**
- `resource_requests` table (v34 migration): a real request-with-fulfillment-lifecycle, separate from the pre-existing bracket-token `resources` status board (v4, "[Beds 30/100]" — unchanged, still a real and different thing: passive current status vs. an active ask). `priority` reuses `messages.precedence`'s exact vocabulary (routine/priority/immediate/emergency) rather than inventing a second one for the same concept. `status`: requested → acknowledged → in_progress → fulfilled, or cancelled. `resource_type` is free text, same precedent as `map_markers.marker_type` — an open-ended real-world list (fuel, medical, food, water, generators, transportation, shelter beds...) that a fixed enum would just be wrong about the first time someone needs something not on it.
- Setting status to `fulfilled` stamps `fulfilled_at`; reverting away from it clears that timestamp rather than leaving it lying around contradicting the current status.
- `ResourceRequestsPanel.tsx` (EmComm → Resource Requests): log a request, change status, assign/clear incident.
- Verified: 7 new tests (47 total, 45 passing + 2 live-only skipped) — default state, rejecting an unknown priority, rejecting a dangling incident reference, fulfilled_at set-then-cleared across a status round-trip, rejecting an unrecognized status, sort order (open requests before closed, most urgent priority first within each group), and real incident assignment + clearing. Clean release build.
- **Explicitly not this slice:** structured SITREP, operational timeline, tactical-map-by-incident. Phase D's last two real pieces.

**Phase D slice 4: operational timeline — built and verified, 2026-09-02**
- `incident_events` table (v35 migration): generalizes the exact pattern `delivery_attempts` (v31) already proved — a real event log, not an inference from scattered `updated_at` columns across `incidents`/`personnel`/`resource_requests`, which can show *that* something changed but not *what* or in what order relative to everything else tied to the same incident. `incident_id` is required here (unlike every other object type's optional one) — a timeline only exists in the context of a declared incident.
- Wired into every incident-touching write path, not added in isolation: `create_incident`/`close_incident` (incident-level events), `set_person_status`/`assign_person_to_incident`, `create_resource_request`/`set_resource_request_status`/`set_resource_request_incident`, `set_message_incident`/`set_marker_incident`. Moving something from one incident to another logs an "unassigned/untagged" event on the old incident and an "assigned/tagged" event on the new one — not just the new state. A status change on personnel/resources not currently tied to any incident logs nothing, correctly — there's no timeline to attach it to.
- Timeline view added to `IncidentsPanel.tsx` — expand any incident to see its recorded sequence.
- Verified: 4 new tests (50 total, 48 passing + 2 live-only skipped) — a full realistic scenario (declare → assign person → status change → resource request → fulfill → tag a message → close) reading back in the exact order it happened with real summary content, moving a person between two incidents logging correctly on both sides, and a status change on an unassigned person logging nothing anywhere rather than writing a dangling row. Clean release build.
- **Phase D now has one real piece left:** structured SITREP (a document combining incident + personnel + resources + traffic), plus tactical-map-by-incident whenever the map panel gets touched.

**Phase D slice 5: SITREPs — built and verified, 2026-09-02**
- `sitreps` table (v36 migration): deliberately a different shape from everything else built today. Incidents/personnel/resource requests/messages/markers all represent *current* state; a SITREP is a permanent, sequence-numbered snapshot of what was known at one moment (real ICS practice — "SITREP #3 as of 14:32Z"), valuable specifically because it doesn't change after the fact. `sequence` counts per-incident (each incident's own #1, #2, #3), not a shared global counter.
- `generate_sitrep_body`: renders real, current personnel/resource/timeline data (filtered to the one incident) into plain text at generation time — the same text serves screen viewing and the "printable report" the roadmap's planning notes describe, no separate rendering path needed for either. Generating a SITREP is itself logged as a timeline event (`sitrep_generated`), so the record of "a report was made" is part of the same timeline the report itself summarizes.
- SITREP generation/viewing added to `IncidentsPanel.tsx`: Generate button, expandable list by sequence, Copy to clipboard (same pattern as the Diagnostics panel's report copy).
- Verified: 5 new tests (55 total, 53 passing + 2 live-only skipped) — real personnel/resource/timeline content actually present in the rendered body, per-incident sequence numbering (not global), rejecting an unknown incident, the generation event itself appearing on the timeline, and sequence-ordered retrieval. Clean release build.
- **Phase D is now backend-complete except tactical-map-by-incident** — a real, separate effort (touching the existing map panel to render incident objects instead of independent pins), not something blocking the rest of what's built.

**Phase D slice 6: tactical-map-by-incident — built and verified, 2026-09-02**
- `grid_square` column added to `personnel` and `resource_requests` (v37 migration) — both already had a free-text `location` field ("the north shelter"), but free text can't be plotted, so this adds the same store-the-square/derive-coordinates-at-read-time pattern the `resources` board already used, rather than storing raw lat/lon that could drift out of sync with the square. `location` and `grid_square` stay separate fields on purpose — a request's location is often known in plain language long before anyone has a grid square for it.
- `set_person_location`/`set_resource_request_location` commands, plus a `GridSquareField` row control in `PersonnelPanel.tsx`/`ResourceRequestsPanel.tsx` — an unparsable square is stored as entered but derives no coordinates (shown inline as "not on map"), matching how the resources board has always handled it, rather than silently rejecting the write or silently producing a wrong pin.
- `TacticalMapPanel.tsx` gained an incident selector ("All incidents" or one specific incident). Markers, personnel, and resource requests are the actual incident objects, so selecting an incident filters those three down to what's tagged to it; mesh nodes, net check-ins, and the generic resources board aren't incident-taggable at all and stay visible regardless of selection, since they're standing infrastructure/presence, not incident state. New legend entries and pin colors for personnel (pink) and resource requests (purple).
- Verified: 5 new backend tests (60 total, 58 passing + 2 live-only skipped) — grid square derives real coordinates, an unparsable square stores the text but derives no pin, clearing a square removes the pin, for both personnel and resource requests. Clean TS build, clean release build.
- **This closes Phase D.** Every slice — incident lifecycle, personnel, resource requests, operational timeline, SITREPs, and now the incident-aware tactical map — is built, tested, and wired together.

---

## Built, but not yet proven in the field

- **Meshtastic direct messaging, synchronized pins, position send/request** — need a second physical node.
- **Winlink/Pat, JS8Call** — verified against real running local software; never verified against an actual RF path or real correspondent station.
- **Hamlib rig/rotator control** — verified against `rigctld`/`rotctld` running locally; not verified against real radio hardware.
- **`MeshTransport`** (this session's work) — unit-tested, not live-tested (no node available).

---

## The missing shared operational core (from the original planning review — status updated)

1. **Canonical operational objects** — ✅ **done for message, marker, incident, personnel, resource_request, and SITREP** (see Phase D above). Alert, station, team, assignment, acknowledgement, and status-event objects are not yet converted — real remaining scope, not urgent until something actually needs to sync one of them.
2. **WayStation Interchange Protocol (WSP/1)** — ✅ **done, 2026-09-02.** See [WSP-1.md](WSP-1.md) for the full spec. WSP/2 (compact/fragmentable RF encoding) is tracked in **Planned backlog** below, deliberately deferred until a real bandwidth-constrained transport exists to need it.
3. **Delivery and transport architecture** — ✅ **done.** The `Transport` trait plus the v31 delivery-attempt history together cover this: every attempt on every transport is now a real, queryable record, not just a latest-state field.
4. **Peer synchronization** — ✅ **done, 2026-09-04.** Reconciliation logic, UI, WSP/1 spec, mDNS discovery, an authenticated TCP transport, and now an opt-in automated background sync loop (see above and [WSP-1.md](WSP-1.md)). On-demand "Sync via Network" per trusted station remains available regardless; the background loop is a separate, explicit per-peer opt-in on top of it, not a replacement.

---

## v2 backlog (deferred until after the offline field test)

**Update, 2026-09-24 — field test concluded, freeze lifted.** Frank reports the real
month-long offline field test is done — roughly two weeks ahead of the original
~2026-10-07 estimate, with no real crashes or data loss beyond what's already fixed
below. Testers gave real feedback (see the two DISCOVERY entries this same update
adds detail to), and Frank's own framing is explicitly "no hurry," not urgent. Active
feature development, including WayStation's own modularization initiative above, is
unblocked as of today; nothing here is forced to move immediately, it's just no longer
a hard-blocked wait. Three concrete items are the natural first pick-ups whenever that
happens: the flight-tracking icon-rotation fix, the radar animation loop + additional
NWS map products, and the scanner multi-profile switcher (all three already scoped in
their own entries below, none newly invented here). A real cross-project consequence
already landed ahead of this, separately from the freeze: Citadel's `vault-api` got a
critical unauthenticated-access fix this same week, which — as an unintended
side-effect — broke the scanner bridge's calls into Citadel; `ae0b9cd` added a real
`citadel_vault_token` header so the bridge keeps working against the now-patched
Citadel, verified live. **One real pattern worth naming across the two field-feedback
DISCOVERY items below, not just two unrelated asks**: both are requests to lean on the
*online* tier harder when it's actually available — an animated radar loop, more real
NWS products, one-tap profile switching — not anything about the RF/offline tiers.
Worth weighing as real signal the next time online-vs-offline feature priority comes
up: testers aren't asking WayStation to be less offline-capable, they're asking the
online half to do more while it's there.

**2026-09-07: Frank is standing up a second, physically separate computer to run WayStation fully offline for a real month-long field test — the actual point of everything built so far. Active feature development pauses here, deliberately: every item below is real, scoped, and not forgotten, but adding more untested surface area right before the test would work against the test's own purpose. Nothing here blocks going offline.** Before cutting over, confirm the offline machine is running latest `main` — the `source_health` launch-crash fix (2026-09-06, see changelog) landed the same session as several other changes, so a stale clone could carry that exact crash into the field. Separately, the hardware-blocked items in **Built, but not yet proven in the field** above aren't "deferred" in this sense — they're already code-complete and waiting on real gear (a second mesh node, an RTL-SDR, a real correspondent station), not on a decision to wait; real field use may exercise some of them for the first time regardless.

**A separate, larger v2 vision landed the same day: see [DESIGN.md](DESIGN.md).** A full page-by-page redesign direction for both WayStation and Citadel (Frank + another Claude session, working from real mockup screenshots), covering a Citadel home-screen redesign (Off-Grid Readiness score, health strip), a genuinely new First Aid & Medical Support subsystem, tab reorganization across nearly every existing WayStation page, and several new cross-feature integrations (POTA→rig, reception report→map, satellite pass→Doppler countdown, aircraft proximity/altitude alerts). None of it is scheduled or broken into real tasks yet — the mockup screenshots themselves still need to be gone through for concrete layout/interaction detail before any of this is buildable work, same as everything else in this section. **Note this is the UI/UX layer of the redesign** — layout, page organization, visual system — distinct from the architecture-layer initiative below, which is about how capability is packaged and toggled, not how it's laid out on screen. The two are complementary, not overlapping.

**A major new architecture initiative, 2026-09-15: give WayStation a real modular/plugin
architecture, matching Citadel's philosophy (not its literal mechanism).** Sparked by
first-hand research on XTOC (see Interoperability & reach below) revealing a much bigger
real feature surface than the original five-gap review found — D-STAR/D-PRS, Mercury HF
ARQ, MeshCore, Reticulum/RNode, FLDIGI, VarAC, ViperGram transports; an integrated RTL-SDR
"Radios workbench"; a governed multi-operator "Shared Workspace/TOC Node" model (viewer/
operator/planner/admin roles, conflict blocking, audit history); 82 native ICS forms with
replay-resistant signing ("Trust Link" — a real capability WSP/1's own signing explicitly
lacks today, see WSP-1.md); drone flight-planning exports (Litchi/QGroundControl/MAVLink/
KMZ/ATAK); DJI/RTSP video ISR; and a more sophisticated per-recipient delivery-tracking
model ("Assured Delivery" — queued/transmitted/ACK/expired/failed, TTL, dedupe, overdue/
silence timers) than WayStation's current delivery-attempt history. Trying to absorb all of
that into one growing monolithic Tauri binary is the wrong shape for it. **Real technical
distinction, not glossed over**: Citadel's actual module mechanism is Docker Compose
profiles — separate containers on a persistent host — which doesn't transfer literally to a
single compiled desktop binary. WayStation borrows the *philosophy* (pick only the
capability you want, don't carry the weight of what you don't) through its own,
WayStation-appropriate mechanism — a real plugin/feature-toggle architecture inside the
Tauri app itself. **Which exact mechanism (a genuine plugin system, compile-time feature
flags with a runtime UI toggle, or something else) is real, unstarted research, not a
decision made here.** Rough three-way division of labor across the family, itself still
open to revision: Citadel hosts new shared/always-on capability as real Citadel modules
(the RTL-SDR workbench, D-STAR/MeshCore/Reticulum, drone/video ISR — matching the same
hardware-hub pattern already proven for weather/maps/scanner); WayStation keeps and
modularizes capability that has to run on the operator's own machine against
locally-attached hardware (existing mesh/Winlink/JS8Call/APRS, plus new local radio
transports); Muster (a new sibling project, a lightweight browser/PWA client — see its own
ROADMAP.md) gets its own frontend module system, consuming both. **Explicit safety
sequencing, Frank's own call**: this is planned now, on a separate branch, and does not
touch the current field-test build — the physically separate offline-test machine keeps
running exactly what it has, testers keep exercising the hardware-side transports
(mesh/Pat/JS8Call/APRS) that stay regardless of any redesign, and none of this work merges
into `main` until the field test concludes. As of 2026-09-15 (nine days into the real
month-long test) no real bugs have surfaced — genuinely good news, worth being honest that
it's encouraging, not yet conclusive, since the test itself isn't finished. **One concrete
action item this research surfaced**: XTOC has a real, currently-shipping feature literally
named "XCAST" (bulletins/video). WayStation's own planned community-bulletin feature (see
Interoperability & reach below) is also named "XCAST" — a real naming collision, same
category of thing this session already caught and fixed for Drawbridge/Outpost/Vanguard on
the Gated/Muster side. Needs an actual naming decision before that feature is built, not
after. **Binding constraint on this whole initiative, added 2026-09-16 (Frank's own concern,
and a real one — read this before touching anything here): WSP/1 is a versioned public
contract Muster (and potentially other future consumers) depends on, not an internal
implementation detail this modularization work is free to change incidentally.**
Modularizing *which capabilities are compiled into WayStation* and *how WSP/1 itself works*
are two separable layers — this whole initiative is about the first one. WSP/1's own spec
already anticipates needing to change safely (`wsp_version`/`wsp_kind` exist specifically so
a future format change is a clean, detectable rejection, not a silent break) — whoever picks
this work up must preserve that: any actual change to WSP/1 needs real backward
compatibility or an explicit version bump, never a silent behavior change a downstream
consumer discovers by breaking. Same rule recorded in Muster's own ROADMAP.md and the
Citadel Ecosystem Master Architecture doc, so it isn't just written once and forgotten.

Reconciled 2026-09-03 against a longer external planning list Frank was carrying separately, item by item against the real codebase rather than taken at face value — some items turned out to already exist elsewhere (WSP/1's own "what's not in WSP/1" section already named three of these), one turned out to already be fully built (offline map tiles, via PMTiles, not the `.mbtiles` the original list assumed), and a few were dropped or narrowed after discussion. Organized by area, not by priority — flight tracking, weather, and the scanner subsystem are the operator's stated top priority given a real high-disaster-risk location, flagged individually below.

**Transport & Networking**
- **WSP/2 compact binary serialization** `[PLANNED]` — already named as deliberately deferred in [WSP-1.md](WSP-1.md)'s "What's not in WSP/1"; today's JSON envelope is fine until a real bandwidth-constrained transport exists to force the issue.
- **Packet fragmentation/reassembly + sliding-window ack** `[PLANNED]` — same WSP-1.md deferral, same reason.
- ~~**mDNS/DNS-SD local peer discovery**~~ — ✅ done 2026-09-03, see that day's changelog. `discovery.rs`: advertises this station as `_waystation._tcp.local.` (callsign in a TXT record, no live port behind it yet — file exchange is still the only real sync transport), browses for others, surfaces them read-only in `SyncPanel.tsx`. **Discovery only, never automatic sync**, per the second non-negotiable principle above — nothing discovered is clickable to sync, moving data still always means Export/Import.
- ~~**Automated background sync loop**~~ — ✅ done 2026-09-04, see that day's changelog and [WSP-1.md](WSP-1.md)'s Transports section. `auto_sync.rs` polls every 5 minutes; a peer only ever becomes a real connect target with both an explicit per-peer `auto_sync` opt-in (off by default, even for an already-trusted peer) *and* current presence in mDNS discovery. On-demand "Sync via Network" is unaffected and still available regardless.
- **WSP/1 sync (manifest/diff/merge) over mesh as its own transport** `[DISCOVERY]`, added
  2026-09-15, surfaced by planning work on Muster (a new sibling project, a lightweight
  browser/PWA ops-coordination tool — see its own ROADMAP.md). Real gap, checked directly
  against [WSP-1.md](WSP-1.md) rather than assumed: WSP/1's actual reconciliation protocol
  (`export_manifest`/`uuids_needed_from`/`export_objects`/`merge_incoming`) only has two
  transports today, file exchange and authenticated TCP (`net_sync.rs`) — mesh is not one of
  them. Meshtastic today only carries the separate outbound `Transport` trait's individual
  messages/markers, not full database reconciliation between two stations. This matters
  beyond Muster: it's also the real prerequisite for this document's own "TTL / multi-hop
  forwarding" gap already named in WSP-1.md's "What's not in WSP/1" section — a station-to-
  station mesh sync transport is a reasonable first step toward that, not a separate
  problem. Not yet scoped into real tasks; needs its own design pass (LoRa/Meshtastic's real
  payload-size ceiling makes this land close to WSP/2's compact-encoding work above, likely
  a shared prerequisite rather than two independent efforts).

**Provenance & Trust**
- ~~**Cleartext HMAC signing**~~ — ✅ done 2026-09-03, see [WSP-1.md](WSP-1.md)'s "Signing" section and that day's changelog entry. Shared-secret-per-known-station model (not a PKI), HMAC-SHA256 over the Objects envelope, verification surfaced (Verified/Unsigned/UnknownSigner/Invalid) but not enforced — the operator decides what to do with an unverified import, same as an unresolved sync conflict. Secret rotation and per-object (rather than per-envelope) signing are real remaining gaps, noted in WSP-1.md rather than silently left out.

**Tactical Ingestion** — operator-flagged critical priority, all three
- **Trunked radio scanner (RTL-SDR + `trunk-recorder`/`op25`)** — **UI and setup done in WayStation, 2026-09-05; conventional-system support added 2026-09-06.** Citadel's `vault-api` serves `/api/scanner` (live status) and `/api/scanner/config` (setup); `citadel_scanner.rs` is the client (reuses `station_profile.citadel_map_host`, same configurable-host pattern the map tiles use), and `ScannerPanel.tsx` (Scanner tab) is both the setup form and the live display — one panel, matching Frank's explicit call that all comms and emergency traffic stay in WayStation rather than splitting across a Citadel-side page (the same reasoning that got Citadel's old `comms.html` deleted). Setup is deliberately generic: any operator's own system data works, whether it's hand-typed off RadioReference's free (no subscription needed) system pages, pulled from digitalfrequencysearch.com's free FCC-license data, or shared on OpenMHz — Citadel validates and writes a real trunk-recorder config regardless of source. **2026-09-06: real conventional (fixed-frequency, non-trunked) system support** — closes a real gap found the moment Frank brought real Sheriff/Fire/EMS frequencies (PANCOM, Donley County, TX) that the original trunked-only builder couldn't express at all (`control_channels` doesn't apply to a system where every channel is its own fixed frequency). Verified trunk-recorder's real `conventional`/`conventionalP25` schema via its own CONFIGURE.md before writing anything (not guessed): a `channelFile` CSV with `TG Number` as a mandatory first column, `Frequency` required, `Tone`/`Alpha Tag`/`Description`/etc. optional. New `build_conventional_config()` + `validate_channel_file_csv()` in `scanner_config.py`; `vault-api`'s `POST /api/scanner/config` now takes a `system_type` (`trunked`/`conventional`/`conventionalP25`) and dispatches to the right builder, saving whichever CSV that type actually uses without touching the other (switching types and back doesn't lose data). `citadel_scanner.rs`/`ScannerPanel.tsx` updated to match: a system-type selector, squelch field for conventional systems, and the response/request CSV field renamed from `talkgroups_csv` to the honestly-generic `csv_data` since it's really two different real shapes behind one name. A real, useful constraint surfaced along the way and worth recording: a single RTL-SDR's realistic capture bandwidth (~2.4MHz) can't cover PANCOM's full 4.7MHz frequency spread in one source -- 8 of its 10 real channels cluster within 1.825MHz and fit fine, but EMS Dispatch (158.7675/158.8425 MHz) sits ~2.9MHz away and needs its own dongle/session for true simultaneous coverage, not a software limitation. Configured and verified live against the real running Citadel instance: the 4-channel PANCOM P25 dispatch cluster (Sheriff Repeater East/West, Fire Dispatch East/West, all real NAC 61F) is the currently active config; the analog set (Sheriff Dispatch + 3 VFD tacticals) and the AEP P25 trunked utility system (two real trunked sites, Clarendon/Hedley) are both verified-working configs ready to paste in whenever the operator switches -- only one system can be active in Citadel's single `config.json` at a time today, a real current limitation (multi-profile switching is real future scope, not built tonight). **Real field confirmation this actually matters, 2026-09-16**: a real storm chaser running WayStation off-grid from his vehicle during the field test asked, unprompted, whether the scanner could work "like a Uniden BearCat" — cycle through a saved set of frequencies and switch between trunked/non-trunked on demand. He doesn't know `system_type` already covers trunked vs. conventional; what he's actually asking for is exactly the multi-profile gap named above — save several real configs (e.g. a trunked county system and a conventional Fire/EMS list) and flip between them without re-filling the whole setup form each time. This is Citadel-side work (`scanner_config.py` would need to store more than one named config and a POST to activate one), not WayStation's — deliberately not started during WayStation's own field-test freeze (~2026-10-07), logged here so it isn't lost.
  ~~**Multi-profile switching**~~ — ✅ **done 2026-09-24**, once the freeze lifted. New pure functions
  in `scanner_config.py` (`build_config`/`validate_csv_for_system` dispatchers, `slugify_profile_name`,
  `unique_profile_id`, `make_profile`) back four new vault-api routes (`GET`/`POST /api/scanner/profiles`,
  `DELETE /api/scanner/profiles/<id>`, `POST /api/scanner/profiles/<id>/activate`) — profiles are stored
  separately from the single live `config.json`/CSV pair trunk-recorder actually reads; activating one
  rebuilds the config fresh from the profile's own stored fields rather than replaying a saved config
  dict, so a later change (e.g. `STATUS_SERVER_URL`) automatically applies to every profile. A direct
  `POST /api/scanner/config` now clears `active_profile_id`, since a manual write isn't tied to any saved
  profile. `citadel_scanner.rs` gained matching commands and `ScannerPanel.tsx` gained a "Saved Profiles"
  section (table of saved setups, Activate/Delete per row, a one-field "save current setup" form) between
  Recordings and Setup — activating a profile also loads its fields back into the form so what's on screen
  always matches what's live. **A real, serious pre-existing bug surfaced while verifying this against the
  actual running stack, unrelated to the new feature itself**: `/api/scanner*` was never proxied through
  cockpit's nginx at all, despite this section's own 2026-09-05/06 entries claiming "verified live against
  the real running Citadel instance" — those verifications hit vault-api directly on its own port, never
  through the actual `citadel_map_host`/8085 path WayStation uses by default, so the whole scanner feature
  (not just profiles) had silently been unreachable through Citadel's real cockpit this entire time. Fixed
  by adding the missing `location /api/scanner`/`location /api/scanner/` blocks to `nginx.conf` (same
  bare-plus-trailing-slash pattern already used for `/api/modules`); a single-file Docker bind-mount
  gotcha meant `nginx -s reload` alone didn't pick it up (the container held the old file's inode) — a
  full container restart was needed, then re-verified for real with `curl` through the actual 8085 path:
  status, list, save, activate, and delete all round-trip correctly now. 19 new Python tests (all passing,
  plus a real Flask-test-client end-to-end run covering save/list/activate/switch/delete), clean TypeScript
  build, clean `cargo check`, all 142 Rust tests still passing. Rebuilt and relaunched the real WayStation
  release binary to confirm the new section actually renders. 9 new Rust tests (2 pure, 2 live -- one for each system shape, both passing against the real stack) and 12 new Python tests (all passing) -- WayStation now 120 tests total, 120 passing, 9 live-only skipped; Citadel's `test_scanner_config.py` now 27 tests, all passing. Clean TypeScript build, clean release build. **2026-09-06 (same day, second session): the statusServer bridge script is done.** Verified trunk-recorder's real websocket status protocol from its own docs before writing anything -- and caught a real design mistake doing so: an earlier draft had the bridge dialing *out* to trunk-recorder as a client, but trunk-recorder's own docs are explicit that trunk-recorder is the client, connecting *out* to a configured `statusServer` URL. Had that shipped, the bridge would never have received a single real message against actual hardware -- caught by re-reading the docs before writing the connection test, not by a field failure. New `scanner_bridge.py`: pure parsing functions for the `systems`/`calls_active`/`recorders`/`rates` message types (two have real documented example payloads, quoted verbatim in tests; two only have documented field *names*, stated plainly rather than implied to be captured payloads), a `ScannerState` accumulator matching trunk-recorder's real independent-message event model, and a real websocket *server* (`run_bridge()`) that trunk-recorder connects to. Both `scanner_config.py` builders now set `statusServer: "ws://scanner-bridge:3010/server"` -- without this, real hardware would run and record calls but never emit a single status message, a real gap that would have made the whole rest of this feature silently do nothing. New `scanner-bridge` docker-compose service (same "hardware" profile as `scanner`, pure software otherwise). Live-verified as thoroughly as this sandboxed environment allows: no USB passthrough here means trunk-recorder itself can't run, but the actual `citadel-scanner-bridge` container was started for real, a real websocket client (playing trunk-recorder's exact role) connected to it over a real TCP socket, sent real message shapes, and the container wrote real `scanner_state.json` to the real shared volume path `vault-api` reads from -- confirmed by reading the file back from both inside the container and the host. Test data cleaned up afterward. `citadel_scanner.rs` gained matching structs (`ScannerSystem`/`ScannerActiveCall`/`ScannerRecorder`/`ScannerDecodeRate`), tested by deserializing the exact JSON captured from that live container run, plus a check that Citadel's pre-2026-09-06 no_data shape still parses cleanly. `ScannerPanel.tsx` gained real Systems/Active Talkgroups (Live)/Recorder Health tables -- deliberately *not* the full System-Status mockup layout: trunk-recorder's statusServer has no per-site breakdown (only per-system) and no recordings-list API exists yet, so neither was built to avoid fabricating structure the real data doesn't support. 2 new Rust tests, 12 new Python tests (11 pure parsing/state + 1 real live server/client socket test, run separately since it needs the `websockets` package the base suite doesn't) -- WayStation now 122 tests total, 122 passing, 9 live-only skipped; Citadel now 39 tests total, all passing (38 in the always-run suite, 1 live-only). Clean TypeScript build, clean release build. 🔲 Still genuinely open, hardware-blocked not code-blocked: an actual RTL-SDR dongle -- everything downstream of that is now real and tested. Real remaining scope, not started: a recordings-list API (nothing currently exposes the real files trunk-recorder writes to `captureDir`), site-level status (trunk-recorder's real message types don't carry it, so the mockup's per-site rows would need a different real data source or stay unbuilt), binding a transcript onto the active incident's timeline via `incident_events` once real transcripts exist, and multi-system-per-source / a config-profile switcher.
- ~~**Flight tracking (ADS-B)**~~ — ✅ **online half done, 2026-09-04** (see that day's changelog and Built-and-verified section below). `flight_tracking.rs` polls OpenSky's free public API in a bounding box around the station's grid square. 🔲 Still open, genuinely hardware-blocked not code-blocked: the local `dump1090`/`readsb` SDR fallback for when the internet's down — same category of gap as mesh multi-node testing needing a second physical node.
  - ~~**"Just a white pin"**~~ — ✅ **done 2026-09-24.** `FlightMap`'s aircraft now render as
    a small rotated plane silhouette (a plain inline SVG via a custom MapLibre `Marker`
    `element`, turned to the real heading with MapLibre's own `rotation`/
    `rotationAlignment: "map"` options — no per-frame transform math needed) instead of a
    flat colored dot. No backend change, exactly as scoped below. The trailing flight-path
    line (the more ambitious ask, needing a real schema change) is still not built — named
    honestly, not silently forgotten.
  - **"Just a white pin" — real, cheap fix identified 2026-09-16** `[DISCOVERY]`, from Keith
    B. Phillips (the same contributor behind the `qrcoder` tool). Checked the actual code
    before answering: `FlightTrackingPanel.tsx`'s `FlightMap` plots every aircraft as a plain
    MapLibre `Marker` colored flat white (`AIRCRAFT_COLOR = "#ffffff"`) with no shape and no
    direction — a fair complaint, that's genuinely all it does today. The real fix is mostly
    free: `true_track` (real heading, degrees) is already fetched from OpenSky, stored in
    `aircraft_tracks`, and returned in `AircraftTrack` — it's just never used visually.
    Swapping the plain marker for a small rotated plane-shaped icon
    (`transform: rotate(${true_track}deg)`) needs no backend change at all. A real, named
    limitation on the more ambitious ask (a trailing flight-path line): `replace_aircraft_tracks`
    does a full `DELETE FROM aircraft_tracks WHERE source = ?` then re-inserts fresh on every
    5-minute poll — no position history is kept per aircraft today, so a trail would need a
    real schema change (a rolling per-`icao24` history, not just the current snapshot), not
    just a frontend change like the icon fix. Deliberately not built during WayStation's own
    field-test freeze (~2026-10-07) — logged so the icon-rotation quick win (and the
    trail's real schema cost) aren't lost.
- **Weather — 3-tier design settled 2026-09-05, online tier built.** Frank's framing: online (web forecast/radar/alerts), RF/offline (NOAA weather radio SAME decode, satellite APT/LRPT imagery), and local (a physical weather-station console) — WayStation should degrade cleanly from the first to the last as connectivity drops, same pattern as everything else in this app. Online tier done this session: `nws.rs` gained real NWS forecast fetching (the documented two-step `/points` → `/gridpoints/.../forecast` flow, migration v41's `forecast_periods` table, same replace-on-fetch pattern as `pota_spots`) and `TacticalMapPanel.tsx` gained a "Show Radar" toggle backed by RainViewer's free, keyless public tile API — both verified live before writing code. `WeatherPanel.tsx` (EmComm tab) is the new dedicated weather page Frank asked for, showing the forecast; radar lives on the map since it's inherently a map layer. Citadel's `/api/weather` (honest no_data default) still exists for the RF/local tiers below — the stale note that it was "shown in comms.html" is corrected here; that page no longer exists (see 2026-09-05 comms.html removal).
  - ~~**Radar loop**~~ — ✅ **animated half done 2026-09-24.** The Weather → Radar tab now
    cycles through RainViewer's real `radar.past` frame sequence (play/pause, a scrubber,
    each frame's real timestamp) instead of showing only the single latest one — the exact
    mostly-UI change this entry below already scoped, reusing data already being fetched
    and discarded.
  - ~~**Watches/warnings polygons**~~ — ✅ **done 2026-09-25.** `nws.rs`'s alert fetch always
    discarded each GeoJSON feature's own `geometry`, keeping only `properties` — the most
    direct of the four named gaps to close, since api.weather.gov was already sending the
    shape. Migration v50 adds `alerts.geometry_json` (nullable — a real alert issued by
    zone/state rather than a drawn polygon genuinely has none). `parse_alert_features` is
    now its own pure function, tested against two real shapes captured live from
    `api.weather.gov` the same day: a real Flood Warning polygon (Gallia/Vinton, OH) and a
    real null-geometry Dense Fog Advisory. The Radar tab reuses the same alert data already
    fetched for the Alerts tab (no second fetch, no third-party WMS overlay like Cloud9's
    own IEM-based one) to draw a GeoJSON fill+outline layer shaded by severity, clickable
    for event/expiry, with a legend. 144 Rust tests passing (2 new), clean TypeScript
    build, clean release build, real app rebuilt and relaunched to confirm it starts clean
    — whether a real polygon actually renders still needs a human to check next time a
    real warning is active nearby, a native window this session can't click through itself.
    Still open: SPC convective outlook, river/flood gauges, current observations — the
    radar animation and this polygon layer close two of the four named gaps, not all four.
  - ~~**SPC convective outlook**~~ — ✅ **done 2026-09-25.** New `spc_outlook.rs`, following
    `space_weather.rs`'s exact poller shape (a national product, not a per-station point
    query like alerts) on a 30-minute cadence. Real, free, keyless GeoJSON verified live
    before writing anything: `spc.noaa.gov/products/outlook/day1otlk_cat.nolyr.geojson`
    already carries SPC's own `fill`/`stroke` hex per risk category (e.g. `LABEL: "TSTM"`,
    `fill: "#C1E9C1"`) — used as-is rather than inventing a color scheme. Migration v51
    adds the `spc_outlook` table, same replace-on-fetch pattern as `forecast_periods`.
    Drawn on the Radar tab beneath the radar/alert layers as broader risk-area context,
    clickable, with its own legend row. 146 Rust tests passing (2 new, against a real
    captured shape plus the real empty-outlook case), clean TypeScript build, clean
    release build. Verified end-to-end for real: rebuilt and relaunched the app, then
    queried its actual live database directly and confirmed the background poller had
    already fetched and stored today's real outlook, matching the live API response.
    Still open from the same field request: river/flood gauges, current observations.
  - **Radar loop + more NWS map resources** `[DISCOVERY]`, 2026-09-16 — real request from a storm chaser running WayStation off-grid in his vehicle during the field test, who offered to name the specific NWS products he wants once asked. Checked the actual code before writing this: `radar.ts`'s `fetchLatestRadarTileTemplate()` already fetches RainViewer's `weather-maps.json` response, which includes a real `radar.past` array (~13 frames, last ~2 hours) — today only the last frame (`frames[frames.length - 1]`) is ever used, the rest are fetched and discarded. A real animated loop is mostly a UI change (cycle through the existing `past` frames on a timer with play/pause/speed controls), not a new data source — RainViewer's response may also carry a `radar.nowcast` array worth checking for short-term forecast frames. Separately, `nws.rs` only wires up two real NWS products today (active alerts as text with no polygons — already flagged as a known follow-up in that file's own module doc — and point forecast text); no SPC convective outlook, no watches/warnings polygons on the map, no river/flood gauge data, no current observations. Deliberately not started during WayStation's own field-test freeze (~2026-10-07) — logging here so the specific product list he offers isn't lost, and scoping each one is its own small piece of work once picked (a different `api.weather.gov` endpoint per product, not one uniform fetch).
  - **RF/offline tier, still `[PLANNED]`:** NOAA weather radio SAME decode — the one genuinely automatable RF source, since all ~1,000+ transmitters nationwide share 7 fixed frequencies; a bundled static transmitter-lookup table (sourced from NWS's public per-state listings) could auto-suggest the local channel from grid square, same zero-setup-cost idea as flight tracking. Satellite APT/LRPT imagery reception is real and more accessible than expected (an RTL-SDR + a simple antenna can pull a NOAA/METEOR-M pass down directly, no internet needed) and could reuse WayStation's existing satellite pass-prediction/Doppler code for timing — but note METEOR-M's LRPT is the current beginner recommendation over the aging APT satellites. Both need an RTL-SDR on Citadel's side, same hardware-hub pattern as the scanner — and if an operator wants scanner + satellite reception at once, that's two separate dongles (one SDR, one job at a time), not a software limitation.
  - ~~**Local tier**~~ — ✅ **done, 2026-09-05.** `weather_station.rs` polls either brand directly over the operator's own LAN — no Citadel, no internet, matching the whole point of this tier. **Ecowitt** (or other Fine Offset-compatible gateway, ~$60-90): real `/get_livedata_info` JSON, parsed defensively since the format is honestly inconsistent (some values carry a separate `unit` field, others embed it directly in the string, e.g. `"3.2 m/s"`). **Davis WeatherLink Live** (~$200+): real no-auth local v1 API. Both normalized to US-customary units (°F/mph/inHg) matching `nws.rs`'s existing convention; one named limitation, not silently guessed past: Davis's rain rate is raw tipping-bucket counts, converted using the standard 0.01in/tip assumption most US Vantage Pro2/Vue stations use, flagged plainly in the UI for anyone with a non-standard collector. Migration v43 (`station_profile.local_weather_brand`/`local_weather_host`, singleton `local_weather_observation` table — same shape as `space_weather`). Configured on the Station Identity form, displayed on the Weather page. Added a genuine 5th `Via` value (`Lan`) alongside the documented `internet|mesh|rf|manual` set — a LAN device is honestly neither of the first two, and mislabeling it would be a real (if not state-machine-breaking) inaccuracy in what the operator sees. 8 new Rust tests against real captured API shapes from both brands — one caught a real bug in the value/unit parser before it shipped (a value with no unit suffix at all was wrongly treated as unparseable). 108 tests total, 103 passing, 5 live-only skipped.
- ~~**APRS/packet radio via Direwolf**~~ — ✅ **fully done 2026-09-06**, both slices. Slice 1 (orchestration: start/stop, status) landed first; slice 2 (real AX.25/APRS decode + TacticalMap markers) landed the same day, second session, once Frank confirmed the priority list and explicitly held WSP/2 until this slice existed as a real bandwidth-constrained transport to design it against. Slice 2's byte-level protocol claims are verified against a real captured frame, not the spec alone: synthesized a real AX.25/APRS packet with Direwolf's own bundled `gen_packets` tool, fed the resulting WAV into a real running Direwolf via stdin, and captured the exact 74 raw bytes it emitted on its KISS TCP port — hand-verified byte-by-byte (destination `APRS`, source `KJ4ESQ-9` with a correctly-decoded SSID, digipeater `WIDE1-1` with the last-address bit correctly set, control `0x03`/PID `0xF0`, then the literal APRS position info field) before writing any parser. That capture surfaced a real harness gotcha along the way, not a Direwolf bug: a client that connects to the KISS port *after* a non-real-time stdin-fed decode has already happened gets nothing — Direwolf doesn't replay a decoded frame to a late-joining client — so the working capture had to hold the KISS port's client connection open *before* feeding any audio in. New `aprs.rs`: `extract_kiss_frames`/`kiss_unescape` (FEND/FESC framing — the unescape rule itself is implemented from KISS's own unambiguous spec, not live-captured, since a literal 0xC0/0xDB byte essentially never appears in ASCII APRS text, said so plainly rather than implied equally verified), `parse_ax25_address`/`parse_ax25_frame` (7-byte shifted-ASCII address fields, SSID in bits 4-1, last-address in bit 0 — the exact layout confirmed above), and `parse_aprs_position` (the uncompressed `!DDMM.hhN/DDDMM.hhW>comment` format only — compressed and Mic-E position encodings are real, different formats not decoded yet, named here rather than silently mis-parsed). Caught and fixed a real bug before it shipped, not after: the position parser's minimum-length guard was 2 bytes short of what its own slicing needed, which would have panicked on a truncated/malformed info field instead of returning a clean `None` — a dedicated regression test now locks that in. Migration v46 adds `aprs_stations` (keyed on callsign, replace-on-heard, same reasoning as `local_weather_observation`'s singleton-row pattern just per-station). `spawn_listener` is a reconnect loop against Direwolf's real KISS port, same shape as `mesh.rs`'s `spawn_poller` — safe to always run unlike `direwolf::start_direwolf` itself, since it's just a TCP client with no audio device involved, and reports honestly as `Unknown` (not `Down`) whenever Direwolf simply hasn't been started yet. `TacticalMapPanel.tsx` gained a new APRS (RF) marker layer, source-tagged with its own color exactly like the mesh/aircraft/personnel/resource-request layers already there, never silently merged with a future APRS-IS (internet-looked-up) source. 12 new Rust tests, 11 of them asserting against the real captured frame byte-for-byte (not a synthetic fixture) — 151 tests total, 139 passing by default, 10 live-only skipped by default (unchanged from slice 1, no new live-only test added here: the parsing pipeline is proven against real captured bytes directly, the same honest-gap category mesh.rs's own live tests describe for anything coupled to a full `AppHandle`/Tauri app state that a unit test can't construct). Clean TypeScript build, clean release build, verified live in the dev-server browser (APRS (RF) appears correctly in the map legend on both the Dashboard's embedded map and the standalone Tactical Map page, no console errors). Deliberately not built alongside this: PTT/transmit support, compressed/Mic-E position decoding, and WSP/2 (now unblocked, still not started this session -- a real bandwidth-constrained RF transport exists now, but designing the compact encoding against it is its own separate piece of work).
**Local AI** — via Citadel's existing Ollama/Kiwix stack, not new infrastructure
- ~~**Offline audio transcription (Whisper.cpp)**~~ — ✅ **done, 2026-09-07.** New `whisper` service in Citadel's docker-compose (real apt-installed `whisper.cpp` 1.8.3, `ubuntu:26.04` specifically since that package doesn't exist in 24.04's repos — confirmed by testing, not guessed), a real downloaded `ggml-base.en.bin` model, `whisper-server`'s real `/inference` HTTP endpoint. Verified live on Citadel's actual hardware before writing any client code: a real 53-second sample clip transcribes in ~5s on the real i7-4770 (no GPU) -- about 10x realtime, chosen specifically so a transcription job shares CPU cores briefly with an Ollama request rather than contending heavily. New `transcription.py` (`list_recordings()` reads trunk-recorder's real `captureDir`, honestly empty since no RTL-SDR dongle exists yet to populate it; `transcribe_file()` posts to the real whisper-server; `is_safe_filename()` rejects path traversal before any filesystem join) backs two new vault-api routes, `GET /api/scanner/recordings` and `POST /api/scanner/transcribe` -- both live-tested against the real running whisper container (success, 404-missing-file, and rejected-path-traversal cases all verified with real curl calls, not assumed). WayStation's new `transcription.rs` is the client (same `citadel_map_host`-based pattern as `citadel_scanner.rs`), and `ScannerPanel.tsx` gained a Recordings section: real file list, a Transcribe button showing the real returned text, and -- closing the explicitly-named remaining scope from the original backlog entry -- a "log to incident timeline" action that calls a new `db::log_transcript_to_incident` command, tagging the real transcript onto an operator-picked active incident's timeline via the existing `record_incident_event` machinery, the same mechanism every other timeline entry in this app already uses. 8 new Rust tests (2 `citadel_base` cases in `transcription.rs`) and 12 new Python tests (`test_transcription.py`, including real temp-directory file listing and path-traversal rejection cases) -- 152 tests total, 142 passing by default, 10 live-only skipped; Citadel's suite now 49 tests, all passing. Clean TypeScript build, clean release build, re-verified the actual release binary runs stable (no crash) with this change. Honest scope line: this transcribes whatever's actually in trunk-recorder's captureDir, which is genuinely empty in this sandbox and Frank's real environment alike until a real RTL-SDR dongle exists -- the full "scanner audio in, timeline entry out" pipeline is real and tested end-to-end with a real (non-scanner) sample WAV standing in for what a real capture will eventually be.
- **Keyword-triggered alert banners** `[PLANNED]` — **deliberately not AI-dependent.** Simple, deterministic keyword/regex matching against transcript text, running locally in WayStation itself, so a "structure fire"/"entrapment" trigger still fires even if Citadel's AI stack is down. Life-safety alerting shouldn't have an AI dependency anywhere in its chain.
- ~~**SITREP synthesizer**~~ — ✅ **done, 2026-09-06.** `ai_sitrep.rs` builds a prompt from an already-generated `Sitrep.body` (real structured data, not a guess — the operator picks which existing SITREP to narrate, nothing is re-derived) and sends it to Citadel's Ollama via the cockpit's `/api/chat` proxy. Verified live against the actual running stack before writing the frontend: a real `curl` round-trip worked, though a cold model load took ~23s on this hardware — dwarfing the ~10s timeout every other Citadel client in this app uses for a status poll, so this one gets its own 120s timeout rather than reusing `citadel_scanner.rs`'s. `IncidentsPanel.tsx` gained an "AI Narrative Summary" button per SITREP, with an honest "can take up to a minute" loading state and the result always labeled "AI-generated — not verified, always check against the SITREP text above," per AI-INTEGRATION.md's rules. Degrades cleanly: the structured SITREP this narrates already works with zero AI involvement, and an unreachable Citadel/Ollama just shows a real error, never a fabricated summary. **Tactical-shorthand compressor is still `[PLANNED]`, not done today** — a separate, smaller effort once there's a real RF-bandwidth-constrained transport to compress for. 4 new Rust tests (2 pure prompt-building checks, 1 `#[ignore]`d live test against the real running Citadel/Ollama that passed, 1 confirming an unreachable host fails honestly) — 116 tests total, 116 passing, 6 live-only skipped by default. Clean TypeScript build, clean release build. Honest gap: the React wiring compiles clean and the underlying Rust path is live-verified, but the actual button hasn't been clicked through end-to-end in the real running app yet (needs a declared incident + generated SITREP to test against, which the no-backend dev server can't provide) — same category of gap as Peer Sync's UI when it first shipped.
- ~~**Local field-reference knowledge base**~~ — ✅ **done, 2026-09-06, re-scoped from "RAG" to real keyword search.** Investigated Kiwix's actual API on the real running `citadel-kiwix` container before building anything: it has no JSON search endpoint and no semantic/embedding retrieval in the version Citadel runs, only full-text keyword search returning an HTML results page meant for a browser. The original backlog wording ("AI-retrieved," "RAG") would have been a real, avoidable inaccuracy about what the operator is looking at, so this shipped as plainly-labeled local library keyword search instead -- true RAG (embeddings + vector retrieval over the ZIM corpus) stays a real, separate, much bigger undertaking, not silently claimed here. New `kiwix_search.rs`: `parse_catalog()` reads the real OPDS v2 feed (`/catalog/v2/entries`) into a book list -- confirmed live against the actual stack, which has Ready.gov, Food for Preppers, Gardening/Mechanics/Woodworking Stack Exchange archives, Project Gutenberg, and more already loaded. `parse_search_html()` hand-parses Kiwix's real (fixed, simple) results template -- deliberately scoped to the `<div class="results">` section before splitting on `<li>`, since the page's own pagination footer also uses `<li>` and would otherwise be misread as fake extra hits. Migration v44: `station_profile.citadel_kiwix_host`, separate from `citadel_map_host` since Kiwix is exposed directly on its own port (8095 by default), not proxied through the cockpit nginx. New `FieldReferencePanel.tsx` (Reference tab): book picker, keyword search, real excerpts, "Open full article" via `tauri-plugin-opener` -- copy explicitly says "not AI, not semantic search" rather than let the feature's origin as a "RAG" backlog item imply more than it does. 6 new Rust tests (4 pure parsing tests against real captured catalog/search HTML fragments, 2 `#[ignore]`d live tests against the actual running Kiwix, both passing) -- 120 tests total, 120 passing, 8 live-only skipped. Clean TypeScript build, clean release build.

**Dashboard**
- **Critical Resource Status chips (Power/Water/Food)** `[PLANNED]` — simple Good/Low/Critical indicators, not a telemetry dashboard. Power pulls from Citadel's Project VIGIL (its existing solar/battery monitor); Water/Food likely from a mix of Citadel's Homestead Logistics and WayStation's own Resources board. Citadel owns the actual hardware polling in every case — WayStation only displays the summary.
- **FEMA/Red Cross shelter intake forms** `[PARKED]` — not needed for the family/friends deployment. Revisit only if WayStation is ever released as a standalone tool for the wider public, where a stranger's install might need a form set beyond ICS-213/309.

**Hardening**
- **Multi-node mesh simulation framework** `[DISCOVERY]` — confirmed hard, not just unbuilt: two `meshtasticd --sim` instances on one host turned out to be fully RF-isolated from each other, no shared virtual medium between them (verified 2026-09-03, see changelog and `mesh.rs`'s test module). A real second physical node, or a from-scratch multi-node simulator, are the only paths left — not a quick config fix.
- **Long-duration true offline soak test** `[🔄 STARTING 2026-09-07]` — multi-day, zero connectivity, profiling cache-retention drift and memory footprint under sustained load. Same item as the Phase A "full no-internet exercise" line above, specified here with more rigor. This is the field test this whole "v2 backlog" pause is for — not itself deferred, everything else is deferred to make room for it.
- **Minimize unsolicited mesh presence beacons** `[DISCOVERY]` — Meshtastic nodes periodically self-announce (NodeInfo/telemetry) even with no human sending anything. Worth investigating whether that's a firmware/device setting already available, or something WayStation itself should ever suppress once it does more than just listen.

**Interoperability & reach** — from a real competitive review of XTOC™ (MKME's commercial tactical-ops software suite), 2026-09-15. Five real gaps, each checked directly against this codebase (grepped, not assumed) before being written down here — see that day's changelog entry for the full comparison and what WayStation already does that XTOC doesn't (a real automatic peer-sync protocol vs. XTOC's manual chunked-text moves).
- **QR-code packet transport** `[PLANNED]` — XTOC converts SITREPs/TASKs into a QR code another device just photographs: zero radio, zero network, works on line-of-sight camera contact alone. Confirmed absent from WayStation entirely (no QR handling anywhere in the codebase).
  **Update, 2026-09-15: real candidate found and verified, not just floated.** Frank's friend (crediting pending — see below) built "QR Bookmark," a working two-app system (creator + reader) doing exactly this: compresses text, splits it across 3 QR panels, and adds a 4th carrying an XOR parity of the other three, so losing *any one panel* to a tear/smudge/thumb still fully recovers the text — real erasure coding, not just "4 QR codes." Tested it directly, not taken on faith: fed it a realistic SITREP-shaped message (not their book-excerpt demo content) and ran its own self-test — passed, "4 panels encoded, decoded (zxing), and rebuilt from every 3." Uses ZXing (real, established open-source barcode library) for decoding, entirely client-side — even its "no app needed, tap a panel's link with a stock camera" convenience path appears to keep data in the URL itself (never sent to a server), the same offline-safe pattern as a cached PWA. Real capacity: ~7863 bytes across the 4-panel card (~2300 usable bytes after compression/parity overhead) — plenty for one SITREP (349 characters used 4% of it), multiple cards needed for anything longer.
  **Real, licensed, and public now: [github.com/keithbphillips/qrcoder](https://github.com/keithbphillips/qrcoder), MIT license** — confirmed directly (`gh repo view`), matches what was tested (`qrbookmark`/`qrreader`, "Offline QR bookmark card creator and reader (static PWAs)"). Built by **Keith B. Phillips**, who has since joined the team — full credit here and in [Citadel's own README.md](https://github.com/frankprobst5-stack/Project-Citadel#credits). Real remaining work: the tool is currently framed for book excerpts (Title/Author/ISBN/Fallback URL fields) — the underlying mechanism is exactly right for tactical messages, but adapting the framing (or porting the core encode/decode scheme natively into WayStation's own Rust code) is real, not-yet-scoped work.
  **Real technical detail from the author's own (2026-09-15-updated) README, worth designing around rather than discovering the hard way**: the offline PWA caching (true airplane-mode use, no page reload needed) only activates once the app has been opened over real HTTPS and installed ("Add to Home Screen") — served over plain HTTP, *including `http://localhost`*, the apps still run but the service worker never registers, so nothing gets cached for that kind of fully-disconnected use. This matters directly for how Citadel would ever host this: Citadel's own dashboard is plain LAN HTTP today (`http://<host>:8085`), which would be enough for on-LAN use (the actual encode/decode logic is 100% client-side JS/WASM regardless, no server calls either way) but would **not** get the tool truly cached for a device that's left the LAN entirely — that specific guarantee needs HTTPS. Also confirmed from the same README: decoding uses ZXing via WebAssembly with jsQR as a fallback (not ZXing alone), real capacity is "about 3,000 words" (2,992-word sample compresses to 7,180 B against a 7,917 B budget), and the two app folders (`qrbookmark/`, `qrreader/`) need to stay side by side since the creator links to `../qrreader/` by relative path.
- ~~**Mobile/PWA build target**~~ **— superseded 2026-09-15, not fixed here.** The single
  biggest structural gap this review found (WayStation is Windows/Linux-only via Tauri's
  native desktop bundling — no iOS/Android target, only `deb`/`AppImage`/`msi`; XTOC ships
  as an installable PWA reachable on any phone). Rather than retrofit Tauri with mobile/PWA
  support, this gap is now owned by **Muster**, a new sibling project (a lightweight,
  purpose-built browser/PWA ops-coordination tool — see its own ROADMAP.md) built the way
  XTOC itself actually is, rather than bolted onto a native-desktop codebase never designed
  for it. Interop between the two goes over WSP/1 (see the new Transport & Networking item
  above for the real, honest state of mesh-as-a-sync-transport).
- **ATAK/CoT (Cursor-on-Target) interoperability** `[PLANNED]` — confirmed absent (no ATAK/CoT/KML handling in the codebase). This is about reach, not features: ATAK already has wide adoption in the same emergency-response/SAR/prepper world WayStation targets, and CoT support means exchanging position/situational data with a tool people may already have installed, rather than asking them to replace it. **Ownership not yet decided, 2026-09-15**: this may end up living in Muster instead (lighter-weight, browser-native, arguably a more natural home for a mapping/interop feature aimed at field use) — not resolved either way yet.
- **Reticulum/RNode/MeshChat interoperability** `[DISCOVERY]` — confirmed absent. A separate, real, actively-used cryptographic mesh stack XTOC bridges to that WayStation's own Meshtastic-based mesh (`mesh.rs`) doesn't talk to. Genuine open question, not a clear yes: worth deciding whether bridging to a second mesh ecosystem is worth the effort against fragmenting focus away from WayStation's own mesh + WSP sync. **Ownership not yet decided, 2026-09-15**: same open question as ATAK/CoT above — may belong to Muster instead of here.
- **Video ISR / drone flight-planning workflows** `[DISCOVERY]` — confirmed absent entirely. A much bigger scope bite than the other four items here, closer to its own subsystem (flight planning + live video ingestion) than a feature add. Worth knowing XTOC has it, not worth chasing unless it comes up as a real, repeated ask.

**"XCAST" community bulletin — WayStation's half (broadcast), 2026-09-15.** Frank's idea, not an XTOC/MKME feature (checked). Citadel authors and displays a simple, public, no-login bulletin (meal times, live charging stations, announcements) for a relief site or community hub during grid-down — see Citadel's own ROADMAP.md entry for that half and the full framing. WayStation's job is getting that content *out*, per the standing rule that all comms/broadcast distribution stays here rather than splitting across a Citadel page. This serves a genuinely different audience than everything else in this list: not a trained operator moving structured SITREPs, just a visitor who needs a plain answer with no app, login, or training. Real delivery options, each reaching a different kind of visitor, none scoped into buildable steps yet:
- A WiFi captive portal — broadcast a local, internet-less network that auto-redirects any connecting phone to the bulletin page. No app install, works on any smartphone.
- An actual low-power AM/FM broadcast (Part 15 — no license needed, the same tech campgrounds/drive-ins use) looping a voice announcement of the bulletin. The one that's literally "a radio station for the community," and reaches anyone with a basic radio, no smartphone or WiFi needed.
- An APRS/packet text bulletin, reusing the real Direwolf/APRS support already in `aprs.rs` — reaches anyone in the community running a ham radio.
`[DISCOVERY]`: real, wanted, needs real investigation into each delivery mechanism's actual feasibility (especially Part 15 broadcast specifics) before this is buildable work.

**Explicitly declined, 2026-09-03**
- ~~Part 97 cleartext compliance toggle~~ — not needed: nothing in WayStation encrypts anything today, so it's already compliant by default. Revisit only if a real encryption feature is ever added that risks getting routed over RF.
- ~~GPS-disciplined time master~~ — low value for what WayStation's revision/timestamp precision actually requires.
- ~~Canonical header migration for `net_roster`/`resources`/`channels`~~ — dropped as a tracked item.

---

## Revised development sequence (Phase A–F)

- **Phase A — Stabilize the current application.** Partial: real regression tests exist for the migration system and the new transport layer; Windows/Linux CI and packaging landed 2026-09-02, first-run onboarding landed the same day (see changelog). A full simulated offline/crash/long-duration exercise is still open — deliberately not attempted with this development session's own network connection live.
- **Phase B — Operational object foundation.** ✅ **Done, 2026-09-01.** Object header, message + marker conversion, revision/provenance rules, WSP/1 documented ([WSP-1.md](WSP-1.md)).
- **Phase C — Two-station communications proof.** ✅ **Done except hardware.** Durable queue + delivery-attempt history (v31), reconciliation logic proven between two real databases, the Peer Sync UI, and the WSP/1 spec are all done. Left: reusing this exact proof over physical mesh/AREDN once hardware exists — genuinely blocked on hardware, not on more design or code.
- **Phase D — Incident operations.** ✅ **Complete, 2026-09-02.** Real multi-incident lifecycle, `incident_id` tagging on messages/markers, `personnel` roster with status + assignment, `resource_requests` with a fulfillment lifecycle, `incident_events` operational timeline wired into every write path, `sitreps` snapshot generation, and the Tactical Map now filters markers/personnel/resource-requests by incident with an "All incidents" default. Personnel and resource requests gained a `grid_square` field (migration v37) so they can plot at all — `location` stayed free text, `grid_square` is the new field that actually derives coordinates, same pattern the `resources` board already used. Nothing left open in Phase D.
- **Phase E — Software-complete application.** 🔲 Not started as a phase, though 2026-09-02/03's work is a down payment on its philosophy: build every hardware-facing capability against simulation/replay/fixtures so real equipment is never a blocker for finishing the software. Most of this phase's real scope is now itemized in **Planned backlog** above — scanner/ADS-B/weather ingestion, local AI, and the multi-node mesh simulation problem are all Phase E candidates once picked up.
- **Phase F — Release candidate and tester deployment.** 🔄 In progress,
  2026-09-14. The "audit for anything that quietly hard-requires Citadel"
  prerequisite mentioned above is done, with real evidence, not just
  checked off: every Citadel host reference (`citadel_map_host`,
  `citadel_kiwix_host`, ports 8085/8095 in `ai_sitrep.rs`,
  `transcription.rs`, `citadel_scanner.rs`, `kiwix_search.rs`, `db.rs`)
  resolves through a safe `.unwrap_or("127.0.0.1:8085")`-style default,
  and every Tauri command returns `Result<_, String>` with `.map_err()`
  turning a Citadel-unreachable network failure into a readable error
  string surfaced to the UI, never a panic. No crash risk found — the
  configurable-host-with-graceful-degrade pattern this file already
  claimed for map tiles genuinely holds everywhere else Citadel is
  referenced too. Also added this session: a real root `README.md` and
  `LICENSE` (GPL-3.0-or-later, matching `package.json`'s declared
  license but previously missing as an actual file — same class of gap
  Citadel had and fixed earlier), and the first real tagged release,
  `v0.1.0`, via the CI pipeline in `.github/workflows/release.yml` that
  had existed but never actually been triggered before now. Known,
  honestly-disclosed gaps going into this release: two `UserManualPanel.tsx`
  sections (mesh/JS8Call/rig-control setup, band-plan reference) are
  deliberately left as honest "not written yet" stubs rather than
  guessed-at ham-radio content, and no macOS build exists (the release
  workflow has no macOS runner configured).

---

## Immediate next priorities

1. ~~**Operational object design**~~ — ✅ done 2026-09-01.
2. ~~**Durable outbound queue with delivery-attempt history**~~ — ✅ done 2026-09-01.
3. ~~**Two-instance proof without new hardware**~~ — ✅ reconciliation logic done and verified 2026-09-01.
4. ~~**A minimal sync UI**~~ — ✅ `SyncPanel.tsx` shipped 2026-09-01 (Settings → Peer Sync). Not yet clicked through by a human in the real running app.
5. ~~**WSP/1 real spec**~~ — ✅ done 2026-09-02, see [WSP-1.md](WSP-1.md). Real versioned envelope (`wsp_kind`/`wsp_version`/`origin_callsign`/`generated_at`), not the bare JSON arrays from the day before — a future format change now fails with a clear error instead of a silent misparse.
6. **Current-state hardening.** ~~CI/packaging~~ — ✅ done 2026-09-02. ~~First-run onboarding~~ — ✅ done 2026-09-02, see changelog. 🔄 The full no-internet exercise starts 2026-09-07 — Frank is setting up a second, physically separate machine to run WayStation genuinely offline for a real month-long field test. Everything else in **v2 backlog** below is deliberately paused for this.
7. ~~**Phase D — Incident operations**~~ — ✅ complete 2026-09-02. All 5 backend slices plus tactical-map-by-incident: the map now filters to a selected incident's markers, personnel, and resource requests, with mesh/net-roster/global-resources staying visible as non-incident infrastructure regardless of selection.

Closing principle, unchanged from the original planning document: **success is not measured by the number of panels. Success is measured by whether WayStation can preserve and move a trustworthy operational picture when ordinary infrastructure fails.**

---

## North-star acceptance scenario

A severe-weather incident begins while internet service is available. WayStation receives alerts and builds the initial local picture. Operators create an incident, check in personnel, and stage communications paths.

The internet then fails. Cached information remains visible and marked with its true age. NOAA radio, Meshtastic, AREDN, local SDR receivers, station sensors, and nearby WayStation peers continue supplying live information.

A shelter reports generator fuel is low. The report becomes a priority resource-request object attached to the incident. WayStation queues it, recommends an available path, records operator approval, sends it, receives acknowledgement, and synchronizes the update to authorized peers.

A field team loses its preferred path. The failed attempt is preserved. WayStation recommends a fallback based on observed availability and policy — it does not silently invent delivery or endlessly retransmit.

A new operator takes over. The incident timeline, outstanding traffic, personnel, resources, station health, and current communications paths provide a complete handoff.

After the exercise or incident, WayStation exports the communications log, messages, acknowledgements, SITREP, timeline, and after-action record.

**Online, WayStation builds awareness. Offline, WayStation becomes the communications infrastructure.**

---

## Changelog

- **2026-09-24 (real color-palette rebrand: dark theme now matches Citadel exactly)** —
  Frank's own follow-up after the 2026-09-18 entry below was corrected for
  overclaiming: "get rid of the green, keep the white and red, use the default
  blues like Citadel, so it looks uniform across the whole suite." Read Citadel's
  own real `:root` values directly from `cockpit/index.html` rather than
  approximating them. The dark theme (`App.css`'s default and primary identity)
  now uses Citadel's literal hex values for background/panel/input
  (`#05090d`/`#071a2c`/`#0a2038`), text (`#e7f1f8`/`#aebbc7`), and its real
  brand accent blue (`#008dff`, was a much lighter `#5aa9e6`); borders and the
  five translucent `--surface-*` overlays are now blue-tinted instead of
  amber-tinted, matching Citadel's own accent hue; `--red` is aligned to
  Citadel's `#e05252` (was `#ff4d4f`, already close); `--amber` needed no
  change — `#ffb000` already matched Citadel's own value exactly. Green is
  gone as a rendered color: `--green` now resolves to the same blue as
  `--blue`, so every one of the ~31 `var(--green)` call sites across the app
  (status dots, connection chips, alert borders, freshness indicators) still
  means exactly what it meant before ("good/quiet/connected") but now renders
  blue instead of green — no component files touched, only the CSS variable's
  value. Four hardcoded green hex literals that bypassed the variable
  entirely (`TacticalMapPanel.tsx`'s net-check-in pin and legend swatch,
  `WorldMapPanel.tsx`'s POTA activator pin, `SatellitePanel.tsx`'s subpoint
  pin) were also updated to the same blue, since a map marker's color can't
  reference a CSS custom property. Caught and fixed a real collision this
  would have otherwise introduced: `TacticalMapPanel.tsx`'s "Resources" pin
  was hardcoded to the *old* `--blue` value (`#5aa9e6`) — once check-ins
  became blue too, the map legend would have shown two near-identical blues
  for two different meanings; recolored "Resources" to gold (`#e8b93f`,
  already an existing theme color) to keep the legend legible. The light
  theme gets the same treatment scoped down: `--green` now resolves to that
  theme's own pre-existing `--blue` (`#2f74b8`) rather than a newly invented
  hex, everything else left alone since Citadel has no light mode to match
  hex-for-hex. **The red (night-vision) theme is deliberately untouched** —
  it was already green-free by construction (every accent there, including
  its own `--green`, is already a shade of red) and introducing a literal
  blue would defeat the one thing that theme exists for, per its own
  standing design rule. Verified live: computed `--green`/`--blue`/`--red`/
  `--bg-app` values checked directly in a real running dev-server browser
  session across all three `data-theme` states (default/light/red all
  correct), plus a clean `tsc --noEmit` and a clean production `vite build`.
  **Correction, same day**: "confirmed no leftover green anywhere" was
  wrong — Frank caught two real misses on the actual Tactical Map that a
  CSS-variable audit alone couldn't find, since neither goes through
  `--green`: `TacticalMapPanel.tsx`'s `APRS_COLOR` (`#2fd4a0`, a teal-green
  RF-station marker, recolored to white — distinct from every other marker
  hue already on that map, and a literal instance of "keep the white" from
  the original instruction) and, the bigger one, `citadelMapStyle.ts`'s
  shared hillshade paint (`hillshade-highlight-color: #3a4a2a`, an olive
  green terrain wash covering the *entire* Tactical Map whenever
  `tactical_terrain.pmtiles` is present). That file's own doc comment says
  it deliberately mirrors Citadel's own `cockpit/map.html`, which carries
  the identical green hillshade value — both files used it, so it read as a
  much larger, more obvious green than any single marker. Fixed
  WayStation's own copy only, per Frank's explicit scoping ("im talking
  about waystation") — Citadel's `map.html` still has the original green
  hillshade, a known, not-yet-addressed follow-up if suite-wide uniformity
  is wanted there too. New values reuse Citadel's own real `--bg`/`--border`
  hex (`#05090d`/`#2a4a66`) rather than inventing new ones, keeping the same
  shadow/highlight contrast the terrain relief needs. Also checked
  protomaps-themes-base's own `dark` theme source directly for further
  green in land-use/park colors — none found, so the vector basemap itself
  isn't a remaining source. Ran a final programmatic sweep of every hex/rgb
  color literal across `app/src` (not just files matching the word
  "green") to catch anything a targeted search would miss — found and fixed
  two more real ones this way, neither named "green" in code: a neon-green
  "hacker terminal" glow on the About panel's action button (`#39ff14` and
  its matching `rgba()` text-shadow/box-shadow values, including a
  `:hover` state that used the same green independently — all retinted to
  Citadel's own `--accent-bright`, `#28c8ff`), and the landmass fill color
  on the canvas-based World Map/Satellite ground-track maps (`#3a5f3a` in
  `lib/mapCanvas.ts`, retinted to Citadel's own `--gray`, `#5b7386`, kept
  lighter than the canvas ocean fill so land/water stay distinguishable).
  Also caught two hardcoded `rgba(57, 217, 122, ...)` text-shadow glows
  (the rig-frequency display and the header clock) that were paired with
  `color: var(--green)` but didn't follow the variable when its value
  changed — fixed to the matching blue glow. Re-ran the full hex/rgb sweep
  afterward: zero genuinely green hues remain anywhere in `app/src`, the
  only surviving matches are inside this changelog's own prose.
  **The actual running app was rebuilt and relaunched to verify this for
  real** — the installed release binary Frank was using still had the
  original pre-edit amber/green look baked in (a real gap all today's
  earlier "verified live" claims missed, since those only checked the
  Vite dev-server preview, never the actual compiled app) — confirmed
  fixed once `cargo build --release` picked up the current `dist/` and the
  app was relaunched.
  **Correction, second pass, same day**: everything above wrongly folded
  WayStation's semantic status color (`--green`, meaning OK/online/
  healthy/connected) into the brand-blue change. The ecosystem's own
  canonical design system (`citadel-ecosystem/brand/PALETTE.md`, written
  2026-09-18 during Citadel's own real 15-page conversion) explicitly
  marks status colors as functional, not brand, and states they don't
  change with a rebrand — Citadel itself keeps green for exactly this
  meaning throughout. Reverted `--green` in both dark and light themes to
  a real green (`#59d67c`, PALETTE.md's own canonical value, not just
  WayStation's prior `#39d97a`/`#1f9d52`, so "connected" now means the
  same color here as in Citadel itself), and fixed the two `.time-bar
  strong`/`.rig-freq` glow shadows that were paired with `var(--green)`
  but hadn't followed it. Brand chrome (bg/panel/border/primary accent)
  and the Tactical Map hillshade stay blue — both were explicit,
  confirmed exceptions, unlike the status color which was never asked
  about on its own and just came along under the general instruction.
  Map-marker categorical colors (net check-ins, POTA activator, satellite
  subpoint) also stay blue — those mark *what kind of thing* a pin is, not
  system health, so PALETTE.md's status-color rule doesn't apply to them.
  Rebuilt and relaunched the real app again to verify. **Scope note**: this is the
  color palette only — `tauri.conf.json`'s `productName`/window title are
  still the bare `"Waystation"`, unchanged, per the correction in the entry
  below. The original 2026-09-05 rationale for the amber/green "command
  center" look (real ham-operator feedback on early mockups) is worth
  remembering if this crosses live testers' desks and draws comment — this
  was Frank's own explicit call to prioritize ecosystem-wide visual
  consistency over that original standalone identity, not a silent drift.
- **2026-09-23 (vault-api compatibility: `citadel_vault_token`)** — Real follow-up to
  Citadel's own vault-api fix (2026-09-21) for a live-reported vulnerability: every
  `vault-api` `/api/` route now requires an `X-Vault-Token` header. `citadel_scanner.rs`
  and `transcription.rs` call those same routes directly (scanner status/config,
  recordings, transcription), so without this fix they'd start getting honest 401s the
  next time the hardware/scanner module is actually enabled. New `station_profile`
  column (migration v49), same manually-entered pattern as `citadel_map_host` — the
  value lives on Citadel's own Settings page, which now displays and offers to copy it,
  not something WayStation can discover on its own. Not an active regression on the one
  live install today (the hardware/scanner module isn't enabled there), but needed
  before that integration is relied on again. 142 tests passing, clean TypeScript build.
- **2026-09-20 (v0.1.1 point release — real System Health fix)** — A live Windows
  tester reported every keystroke on the first-run setup screen taking 30-60 seconds,
  escalating to a full "Not Responding" freeze. Root cause traced to
  `system_health.rs`'s 3-second poller reconstructing its `Disks`/`Components` lists
  from scratch every cycle — a full disk/volume re-enumeration plus, on Windows, a WMI
  hardware-sensor re-query, both known to occasionally stall for seconds, worse
  alongside Docker Desktop's WSL2 virtual disks. Fixed by keeping one persistent
  `Disks`/`Components` instance in `SystemHealthState` (the same pattern this file
  already used correctly for `System`) and calling the real incremental `.refresh()`
  instead of `new_with_refreshed_list()` every cycle — confirmed against `sysinfo`
  0.32's actual source that `.refresh()` takes no arguments and doesn't re-enumerate,
  just refreshes already-known entries. This is the first real point release since
  `v0.1.0`: version bumped to `0.1.1` across `package.json`/`Cargo.toml`/
  `tauri.conf.json`/`Cargo.lock`, tagged and shipped through the existing
  `release.yml` pipeline. Genuine, real field-test value: this is exactly the kind of
  bug the offline field test exists to surface, and it surfaced fast.
- **2026-09-18 (partial: new app icon + README logo, not a real rebrand)** —
  Regenerated WayStation's full Tauri icon set (desktop `.ico`/`.icns`, all PNG sizes,
  Windows Store logos) from the new ecosystem emblem via the official `tauri icon`
  tool, and added the brand lockup image to the top of the README. **Corrected
  2026-09-24 — this was mischaracterized as "the rebrand is done" and it isn't:**
  `tauri.conf.json`'s `productName` and window `title` are both still the bare
  `"Waystation"`, and there is zero mention of "Citadel Ecosystem" anywhere in the
  actual app source (`app/src` — checked directly, not assumed). What actually
  happened is an icon swap and a README image; the app itself — window title, any
  in-app about/brand surface, the product name a user actually sees while running it —
  still presents as standalone WayStation. A real rebrand (product name, window title,
  in-app branding) is still open work, not done, and the ecosystem-wide vector brand
  crest itself remains unbuilt per the Master Roadmap's own Stage 7.
- **2026-09-15 (Muster spun off, WSP/1-over-mesh gap surfaced — documentation only, field
  test unaffected)** — Planning work on a new sibling project, **Muster** (a lightweight
  browser/PWA ops-coordination tool answering XTOC's confirmed mobile/PWA-reach advantage —
  see Muster's own ROADMAP.md), surfaced a real gap in this codebase worth recording here
  rather than only in Muster's own document: WSP/1's actual sync protocol
  (`export_manifest`/`uuids_needed_from`/`export_objects`/`merge_incoming`) only has two
  transports today, file exchange and authenticated TCP — **mesh is not one of them**,
  confirmed by reading `WSP-1.md` directly rather than assumed. Added as a new `[DISCOVERY]`
  item under Transport & Networking, framed as a shared prerequisite with WSP-1.md's own
  already-named "TTL / multi-hop forwarding" gap, not a separate ask. Retired the "Mobile/PWA
  build target" gap from the Interoperability & reach list — no longer WayStation's own item
  to solve, superseded by Muster's existence — and flagged ATAK/CoT and Reticulum/RNode/
  MeshChat interoperability as items whose ownership (WayStation vs. Muster) isn't decided
  yet. Documentation only, consistent with the standing 2026-09-07 pause on *active feature
  development* for the offline field test — nothing here adds new code or untested surface
  area to the machine currently running that test.
- **2026-09-07 (pausing active development for the real offline field test)** — Frank asked whether the three items left from the session's confirmed goal list (keyword alert banners, Power/Water/Food dashboard chips, canonical-object conversion) were genuinely everything open — they weren't; laid out the full honest picture instead of the trimmed list (WSP/2 + packet fragmentation held, the RF/offline weather tier and several items genuinely hardware-blocked, two DISCOVERY-only investigation items, the long-planned offline soak test itself). Frank's call: fold all of it into a clearly-marked v2 backlog and stop active feature work here — he's setting up a second, physically separate computer right now to run WayStation fully offline for the real month-long field test this whole project has been building toward. Agreed this was the right call: more features tonight would only add untested surface area right before the point of the exercise is to find out what actually holds up unattended. **Renamed "Planned backlog" to "v2 backlog (deferred until after the offline field test)"** with a framing note explaining the pause and flagging the one real operational risk: the `source_health` launch-crash fix landed *this same session* (see the 2026-09-06 entry below), so the offline machine needs to be running latest `main`, not an earlier clone, or it will carry that exact crash into the field. Re-tagged the "long-duration offline soak test" backlog line as `🔄 STARTING 2026-09-07` rather than leaving it listed as just another deferred `[PLANNED]` item alongside everything actually being paused — it's the reason for the pause, not itself paused. Updated **Immediate next priorities** item 6 to match. Documentation only, no code changed.
- **2026-09-07 (offline audio transcription, Whisper.cpp)** — Second item on the goal list Frank confirmed the prior session. Investigated real install options before designing anything, same discipline as the Direwolf work: `whisper.cpp` is a real Ubuntu `universe` package (1.8.3), but only on `26.04` -- confirmed by actually testing `ubuntu:24.04` in a throwaway container first and finding apt-cache genuinely empty there, not assumed. Frank asked a real question before installing -- how a second CPU-bound local-AI workload would affect Ollama on Citadel's modest hardware (i7-4770, no GPU) -- answered with real numbers pulled from the live host (4c/8t, Ollama idle at 0% CPU/38MB between requests) rather than a generic answer, landing on `base.en` specifically because a real 53-second sample clip transcribes in ~5s on that CPU, brief enough that even a worst-case overlap with an Ollama request stays a short, transient slowdown rather than a real contention problem. New Citadel `whisper` docker-compose service (apt-installs the package and downloads the real ~141MB model into a persisted volume on first boot -- no hardware profile needed, unlike `scanner`, since this is pure CPU inference with no USB device involved), `transcription.py` (`list_recordings()`/`transcribe_file()`/`is_safe_filename()`), and two new vault-api routes -- all live-tested end to end against the real running whisper container before being called "done": listed real files, transcribed a real audio file through the full stack, and confirmed both the missing-file and path-traversal-rejection error paths return real, useful error text. WayStation's new `transcription.rs` client plus a Recordings section on `ScannerPanel.tsx` close the loop, including the explicitly-named remaining piece from when this item was first scoped: a real "log to incident timeline" action (`db::log_transcript_to_incident`) that tags a transcript onto an operator-picked active incident via the same `record_incident_event` machinery every other timeline entry already uses. See the Planned Backlog entry below for the full technical writeup, including the honest scope line: this transcribes whatever's actually in trunk-recorder's captureDir, which stays genuinely empty until a real RTL-SDR dongle exists -- verified with a real (non-scanner) sample WAV standing in for what a real capture will eventually be, not a scanner-specific claim. 152 tests total, 142 passing by default, 10 live-only skipped; Citadel's suite now 49 tests, all passing. Clean TypeScript build, clean release build, re-verified the actual release binary launches and stays stable with this change (no repeat of the crash fixed earlier this session).
- **2026-09-06 (real launch-crash fix: `source_health.via` CHECK constraint)** — Frank reported WayStation crashing / not loading when launched from Citadel's dashboard tile. Reproduced directly by running the actual release binary in a terminal rather than guessing: it aborted (SIGABRT, core dumped) within seconds. The real log showed the full chain: `connectivity.rs`'s `report_source_health` panicked on `SqliteFailure(ConstraintViolation): CHECK constraint failed: via IN ('internet','mesh','rf','manual')` -- `source_health.via`'s CHECK constraint was set once at the table's original creation (migration v1) and never updated when `Via::Lan` (2026-09-05) or `Via::Aprs` (2026-09-06, this same session) were added to the enum. The panic happened while holding the shared `db` mutex, which poisoned it; every other background poller thread (rig, pat, rotator, direwolf, flight_tracking, dxcluster, the main thread itself) then panicked too on their own next `db.0.lock().expect(...)` call -- pure collateral damage from one poisoned mutex, not six separate bugs. One of those panics happened to land inside a WebKit C callback (`register_uri_scheme`) that Rust cannot unwind across, which is what actually took the whole process down with `abort()` rather than just losing a few background threads. Checked whether any of the seven *other* tables sharing this same stale-looking CHECK text (alerts/space_weather/contests/psk_spots/pota_spots/dx_spots/satellite_tles) could hit the same failure before assuming `source_health` was the only real fix needed -- confirmed by reading every one of their real `INSERT` statements that they all hardcode the SQL literal `'internet'`, never a bound `Via` parameter, so they're structurally incapable of this failure regardless of how many variants `Via` grows to; only `source_health` (written by every integration's poller via `report_source_health`, taking a live `Via` argument) was actually broken. Migration v47 rebuilds `source_health` with the full current variant set (SQLite has no `ALTER TABLE ... DROP CONSTRAINT`) -- applied live against Frank's real, months-old database (backed up first to `waystation.db.pre-v47-backup-<timestamp>`) and confirmed clean: no data loss, `source_health` immediately started accepting real `aprs`-tagged rows from the pollers that had been silently crashing the app since they were added. New regression test, `report_source_health_accepts_every_real_via_variant`, calls it with every current `Via` variant against a freshly migrated database -- written specifically so the *next* new `Via` value can't reintroduce this exact class of bug by being added to the enum without the CHECK constraint following it. 150 tests total, 140 passing by default (no `#[ignore]`s touched), 10 live-only skipped. Verified by re-running the actual release binary against the real (now-migrated) database for a full 8 seconds with zero panics, versus the immediate abort before the fix. This bug had almost certainly been silently crashing every real launch since `Via::Lan` shipped 2026-09-05, not just since tonight's Direwolf work -- the dev-server browser verification used throughout this project never actually exercises the native background pollers the way a real launched app does, which is exactly how this went unnoticed until Frank hit it for real.
- **2026-09-06 (Direwolf/APRS decode, slice 2 — feature now fully done)** — Frank confirmed a working goal list for the session (five items) and, on the one genuinely conflicting item, agreed to hold WSP/2 until this slice actually existed as a real bandwidth-constrained transport to design against, rather than build it speculatively first. Picked this up first since it both closes the Direwolf backlog item and unblocks that hold. Every byte-layout claim in the new code is checked against a real captured frame, not the KISS/AX.25 spec alone: synthesized a real AX.25/APRS packet (`KJ4ESQ-9>APRS,WIDE1-1:!3521.50N/10130.20W>...`) with Direwolf's own bundled `gen_packets` tool, fed the WAV into the real installed Direwolf via stdin, and captured the exact 74 raw bytes it sent on its KISS TCP port -- hand-decoded byte-by-byte before writing a single line of the parser (destination/source/digipeater addressing, the SSID and last-address bits, control/PID, the literal APRS info text). Hit a real methodology snag first: an early capture attempt got zero bytes despite Direwolf successfully decoding, because feeding audio via stdin isn't real-time -- decode happens almost instantly, and Direwolf doesn't replay a frame to a KISS client that connects after the fact. Fixed by holding the client connection open (via a FIFO) before feeding any audio in, capturing it live once, then it was ground truth for every test after that. New `aprs.rs`: `extract_kiss_frames`/`kiss_unescape` (FEND/FESC framing -- escaping itself implemented from KISS's own unambiguous spec rather than live-captured, since a literal 0xC0/0xDB essentially never shows up in ASCII APRS text, and said so plainly in the module doc rather than implied equally verified), `parse_ax25_address`/`parse_ax25_frame`, and `parse_aprs_position` (uncompressed `!DDMM.hhN/DDDMM.hhW>comment` format only -- compressed and Mic-E encodings are real, different, not decoded yet, named rather than silently mis-parsed). Caught and fixed a real bug before it shipped: the position parser's length guard was 2 bytes short of what its own slicing needed, which would have panicked on a truncated info field instead of failing cleanly -- a dedicated regression test locks the fix in. Migration v46 adds `aprs_stations` (keyed on callsign, replace-on-heard). `aprs::spawn_listener` reconnect-loops against Direwolf's KISS port, same shape as `mesh.rs`'s poller -- safe to always run (unlike `direwolf::start_direwolf`, no audio device involved), reports `Unknown` rather than `Down` while Direwolf just hasn't been started yet. `TacticalMapPanel.tsx` gained a source-tagged "APRS (RF)" marker layer, same convention as the existing mesh/aircraft/personnel/resource-request layers, never merged with a future APRS-IS source. 12 new Rust tests, 11 asserted against the real captured frame byte-for-byte -- 151 tests total, 139 passing by default, 10 live-only skipped (unchanged; the parsing pipeline is proven directly against real bytes rather than needing a new live test coupled to a full `AppHandle`, the same honest category of gap mesh.rs's own live tests describe). Clean TypeScript build, clean release build (`localhost:1420` count still 1), verified live in the dev-server browser -- the new legend entry renders correctly on both the Dashboard's embedded map and the standalone Tactical Map page, no console errors. Not built this slice, named rather than silently skipped: PTT/transmit, compressed/Mic-E position decoding. WSP/2 is now genuinely unblocked but not started -- a real transport to design against now exists, designing the compact encoding itself is separate work.
- **2026-09-06 (Direwolf/APRS orchestration, slice 1)** — Frank asked to start on the Direwolf backlog item; investigated hardware/install status first rather than assuming (checked `which direwolf` — not installed) and asked which state he was actually in. He chose to install it for real: `sudo apt-get install direwolf` (I can't run `sudo` myself, no TTY for the password prompt, so talked him through running it), landing the real Ubuntu `universe` package, version 1.8.1, with `hamlib`/`gpsd`/`cm108-ptt` support built in. Rather than write the wrapper against assumed config directives, ran the real binary first: pulled Direwolf's own annotated `conf/generic.conf` from its GitHub repo to confirm `AGWPORT`/`KISSPORT`'s real default ports (8000/8001), then actually launched it with a throwaway config and a real capture device (`hw:1,0`, this machine's built-in mic) — caught a genuinely non-obvious real behavior this way: Direwolf's KISS TCP listener stays bound to its hardcoded default port 8001 *in addition to* whatever `KISSPORT` is configured, while `AGWPORT` doesn't share that quirk and binds cleanly to just the custom port. That finding shaped the design: both ports pinned to a WayStation-owned pair (8010/8011) rather than Direwolf's real defaults, so a manually-run copy elsewhere never collides. Split the feature into two slices rather than building the whole backlog item in one sitting, same reasoning Phase D used for its five slices: this entry is orchestration only (start/stop, status, config generation) — real AX.25/APRS decode and TacticalMap markers are a separate, larger slice, deliberately not started. New `direwolf.rs` follows Pat/JS8Call's established shape exactly (`find_direwolf_binary`, a pure `build_config`, `start_direwolf`/`stop_direwolf`/`get_direwolf_status` commands, a health poller). One deliberate deviation from Pat's pattern: this never auto-starts at app launch — Direwolf opens a real audio capture device (a live microphone) the instant it runs, even RX-only with no PTT configured, and turning that on silently on every launch before an operator has confirmed audio routing would be a real, avoidable surprise; Start/Stop is an explicit action on the new `PacketPanel.tsx` (Messaging tab). Added the real 6th `Via` value (`Via::Aprs`) this backlog item's own text called for back on 2026-09-05, rather than reusing `Via::Rf` — a packet/APRS network (many stations heard passively) is structurally different from the point-to-point RF messaging sessions `Rf` already represents for JS8Call/Winlink. Migration v45 adds `station_profile.direwolf_audio_device` (Station Identity panel — run `arecord -l` to find real device names; blank uses Direwolf's own default). 6 new Rust tests: 5 pure `build_config` cases (callsign uppercasing/trimming, `ADEVICE` correctly omitted for `None` and for a saved-but-blank string, pinned ports always present) plus a real `#[ignore]`d live test — bypasses the tauri `AppHandle`/`State` layer entirely, same technique mesh.rs's live tests use, spawns the actual installed binary directly and confirms its AGW port becomes reachable then actually closes on kill. Ran it for real against the real binary: passed. 137 tests total, 127 passing by default, 10 live-only skipped. Clean TypeScript build, clean release build (`localhost:1420` count still 1). Verified live in the dev-server browser as far as that environment allows: the Packet panel renders and shows the same honest "Loading…" state Winlink/JS8Call already show there (`invoke()` has no real Tauri backend to answer outside the native app — an existing, known limitation, not a regression), no console errors, all three themes unaffected.
- **2026-09-06 (Scanner page visual design, matches Frank's approved "System Status" mockup)** — Closes the last open piece of the statusServer-bridge work: the page was functionally complete (real systems/active-calls/recorder-health data flowing end to end) but still rendered in unstyled, no-CSS markup — confirmed by grepping `App.css` for the classes `ScannerPanel.tsx` was actually using (`.panel-sync`, `.sync-section`, `.sync-peer-list`, `.band-table`) and finding zero matches for any of them. Frank shared the mockup screenshot twice asking for the page to match it. New `scan-*` CSS classes (same separate-prefix discipline as the Dashboard's `.dash-*` set, so nothing here reskins another tab) bring the mockup's visual language — icon header, status pill with a colored dot, stat tiles, styled tables with a hover state — onto real theme tokens, verified in all three themes (dark/light/red) by reading computed styles live, not just eyeballing one. The mockup itself was not copied wholesale: its Sites/Recordings-24h/Disk-Space/Uptime stat tiles, Quick Controls (Start/Stop Recording), Recording Options, Recent Recordings list, and Storage Usage donut all have no real API behind them today, so instead of shipping them with placeholder numbers or dead buttons, they're left out entirely — only Systems/Active Talkgroups/Recorder counts (all real, from `citadel_scanner.rs`'s existing structs) became stat tiles, and the Systems table gained a real, derived "Active TGs" column (`active_calls` filtered by system name) rather than the mockup's unbacked per-site breakdown. The panel definition also gained `width: "full"` (was default/standard) since the mockup's multi-column tables need real horizontal room the single-grid-column width never had. Verified live: `npx tsc --noEmit` clean, `cargo test --lib` 122/122 passing (no Rust touched, confirms no regression), release build clean, and — since `invoke()` can't be called outside a real Tauri window, the same known dev-server limitation noted throughout this project — checked the rendered page directly via the accessibility tree and computed styles instead of a screenshot: confirmed the honest "Could not reach Citadel: ... reading 'invoke'" error state renders correctly styled, confirmed a throwaway test table (injected and discarded, not part of the real data path) laid out correctly at full width, and confirmed pill/title colors recompute correctly across all three themes. No Rust or Python changes — pure CSS/TSX.
- **2026-09-06 (scanner: real conventional-system support, real PANCOM/AEP config)** — Frank brought real Sheriff/Fire/EMS frequencies for Donley County, TX (PANCOM split-repeater systems) and an AEP utility trunked system, and asked to actually configure the scanner with them. The existing scanner setup (2026-09-05) only knew how to build trunked P25 configs -- PANCOM's individual fixed-frequency dispatch/tactical channels had no way to express in that shape at all. Verified trunk-recorder's real conventional-system schema from its own CONFIGURE.md before writing code (`WebFetch` against the real docs, not memory): `channelFile` CSV needs `TG Number` as a mandatory first column, `Frequency` required, everything else optional. Built `build_conventional_config()`/`validate_channel_file_csv()` in Citadel's `scanner_config.py`, extended `vault-api`'s `/api/scanner/config` to dispatch by a new `system_type` field, and updated `citadel_scanner.rs`/`ScannerPanel.tsx` to match -- including renaming the CSV field from `talkgroups_csv` to `csv_data` since it's honestly two different real shapes now, not one. Along the way, computed the real frequency spread of Frank's supplied channels and found a genuine hardware constraint worth surfacing rather than silently ignoring: PANCOM's full channel list spans 4.7MHz, wider than a typical RTL-SDR's realistic ~2.4MHz capture window, but 8 of its 10 real channels cluster within 1.825MHz and fit fine -- only EMS Dispatch sits far enough away (~2.9MHz) to need a separate dongle/session. Verified every real piece live against the actual running Citadel stack rather than assuming: `docker exec` into the real `library.xml`-adjacent scanner container config to confirm file paths, a live `curl` round-trip for both the new conventional route and the pre-existing trunked route (regression-checked, still works under the renamed field), and finally saved Frank's real 4-channel PANCOM P25 dispatch cluster (Sheriff Repeater East/West, Fire Dispatch East/West, real NAC 61F) as the actual active Citadel config, confirmed by reading it back. The analog channel set (Sheriff Dispatch + 3 VFD tacticals) and the AEP trunked config are both verified-working and ready to paste in when Frank wants to switch -- only one system can be active in Citadel's single config file at a time today, named honestly as a current limitation rather than glossed over. 9 new Rust tests (WayStation: 120 total, 120 passing, 9 live-only skipped) and 12 new Python tests (Citadel: 27 total, all passing). Clean TypeScript build, clean release build.
- **2026-09-06 (local field-reference library search)** — Second backlog item picked up in the 4-day pre-offline-test push, right after the SITREP synthesizer. Investigated Citadel's real running `citadel-kiwix` container before writing code: found it has no JSON search API, only an HTML results page, and confirmed via `docker exec ... cat /data/library.xml` that the search API needs the book's raw UUID (`books.id=`), not the catalog's `<name>` field it looked like it should accept at first (that returned "no such book" -- a real dead end caught before it became a silent bug). Once the real request shape was confirmed working by hand (`curl` against a real Ready.gov query returned 40 real hits), re-scoped the backlog item's own language: it had said "RAG" and "AI-retrieved," but there's no embedding/semantic retrieval anywhere in this Kiwix version -- shipping honest "local library keyword search" instead of claiming AI involvement that doesn't exist. New `kiwix_search.rs` hand-parses both the real OPDS catalog feed and the real search-results HTML (no new HTML-parser dependency for one simple, stable format) -- the search parser deliberately scopes to the `<div class="results">` section first, since Kiwix's own pagination footer reuses `<li>` and would otherwise read as fake additional hits. Migration v44 adds `station_profile.citadel_kiwix_host` (Kiwix has its own exposed port, not proxied through the cockpit nginx like scanner/weather/chat). New `FieldReferencePanel.tsx` under Reference: book picker, search box, real excerpts, opens the full article via `tauri-plugin-opener`. 6 new tests, including 2 `#[ignore]`d live ones that both passed against the real running stack -- 120 total, 120 passing, 8 live-only skipped. Clean TypeScript build, clean release build.
- **2026-09-06 (SITREP AI narrative synthesizer)** — Frank asked to push through as much of the backlog as possible in the 4 days before a planned month-long offline field test. Picked this item first because it was the only Local-AI item not blocked on hardware or Citadel-side infrastructure that doesn't exist yet: real Citadel stack confirmed already running (`docker ps` showed `citadel-ollama`, `citadel-cockpit`, etc. live), and a real `curl` round-trip against `http://127.0.0.1:8085/api/chat` (the cockpit nginx's real proxy to `citadel-ollama:11434`) confirmed the whole path works before any code was written -- the only real model pulled is `llama3.2:1b`, and a cold load took ~23s, a genuine constraint the client code and UI both had to account for honestly rather than assume away. New `ai_sitrep.rs`: `build_prompt()` is a pure function (real SITREP text embedded verbatim, plus an explicit instruction not to invent facts not present in it) so it's testable without the network; `synthesize()` does the real HTTP call with its own 120s timeout, not `citadel_scanner.rs`'s 10s one, which would have failed on a cold load. `generate_sitrep_narrative` command takes an existing `Sitrep.body` directly rather than an incident_id -- it narrates a SITREP that already exists, it never re-derives or regenerates one of its own. `IncidentsPanel.tsx` gained an "AI Narrative Summary" button per SITREP with an honest "can take up to a minute" loading state and the result always visibly labeled as AI-generated and unverified, per AI-INTEGRATION.md's standing rules. 4 new tests, including a real `#[ignore]`d live call that actually passed against the running stack -- 116 total, 116 passing, 6 live-only skipped. Clean TypeScript build, clean release build. Honest gap: the frontend button itself hasn't been clicked through end-to-end yet (needs a real declared incident + generated SITREP, which the no-Tauri dev server can't provide) -- the Rust path all the way to real generated text is what's actually been verified live.
- **2026-09-05 (real composite Dashboard, built from Frank's approved mockup)** — Closes the dashboard redesign loop: Frank shared a mockup he liked, WayStation Design recreated it, family reaction to screenshots was positive, and Frank asked to "keep it and wire it up" -- with an explicit "don't want no fake data" condition. Every number on the shipped page traces to a real command; nothing invented. Two backend pieces genuinely didn't exist before today and were built for real rather than faked: `system_health.rs` (new module, real CPU/memory/disk via the `sysinfo` crate already a dependency for `pat.rs`'s process detection -- disk reported is specifically whichever real mount contains WayStation's own data directory, not an arbitrary "first disk"; CPU temperature only ever shown when a real sensor label matches, honestly omitted otherwise) and a real cross-app "Recent Activity" feed (`db::get_recent_activity`, merging and sorting the most recent rows already sitting in `delivery_attempts`/`incident_events`/`mesh_messages`/`messages` rather than inventing a new table every ingest site would need to remember to write into). Also added `db::get_traffic_counts` (a real `GROUP BY precedence` query plus a checked-in roster count) for the Incident Status card's traffic section -- deliberately shows all four real precedence values (including `immediate`, which the original mockup's three-row design would have silently dropped) rather than matching the mockup's row count at the cost of a real category. `DashboardPage.tsx` (new, `src/`, not under `panels/` since it isn't a registry panel) assembles: `useConnectivity()` for Radio/Rotator/Winlink/JS8Call/Mesh/Internet chips, `citadel_scanner`'s live status for Scanner, the real `TacticalMapPanel` embedded directly (not reimplemented) for the map, `incident_info` + the new traffic counts for Incident Status, real `rig`/`rotator` status for the quick cards (mockup's fabricated SWR bar graphic was dropped for a real S-meter reading instead, since no real SWR value exists anywhere in this app), the new recent-activity feed, real space/local weather, and the operator's own Channel Directory for Tactical Nets (honest empty state if none are configured yet). App.tsx now special-cases the "dashboard" tab to render `DashboardPage` directly instead of the old generic per-category panel stack, since a fixed composite layout and a grid-of-independent-panels are fundamentally different things; the five panels that previously lived under the "dashboard" category (world map, rig control, rotator control, active alerts, space weather) moved to whichever existing category actually fits them (tactical-map, tools ×2, incident-ops, activity) rather than becoming unreachable. All new `.dash-*` CSS classes are deliberately separate from the existing `.panel`/`.panel-title`/`.pill` classes the rest of the app already uses -- the mockup's original standalone CSS used those exact names and would have silently reskinned every other tab if reused verbatim. 8 new Rust tests (system_health's two pure decision functions, traffic-counts, recent-activity merge/sort/limit) -- 113 tests total, all passing (5 live-only skipped, unchanged). Clean TypeScript build, clean release build. Verified live: ran the real dev server, clicked through every nav link including the Dashboard's own "Full Screen"/"Open" buttons, confirmed no console errors and every section renders an honest empty/unknown state (never a fake number) when `invoke()` has nothing real to report outside a full Tauri session.
- **2026-09-05 (Direwolf/APRS recovered into backlog)** — Frank asked whether Direwolf had been covered anywhere, suspecting it was lost when an earlier batch of planning docs was deleted. It was: found named repeatedly across surviving planning material (both `WayStation_Master_Development_Roadmap` PDFs, and two preserved planning-conversation `.txt` files) as a Phase 3/off-grid-messaging item — "APRS/Direwolf waits on an audio interface" — but it had never made it into this document. Frank then supplied the actual design intent behind it, not previously written down anywhere: Direwolf as an orchestrated backend service (same treatment as Pat/JS8Call) rather than its own screen, with real RF-decoded APRS positions feeding the Tactical Map directly as a source-tagged marker type (RF vs. APRS-IS vs. mesh, never silently merged) — the differentiating feature being live RF station positions with no internet involved at all. Added as a new `[PLANNED]` item under **Tactical Ingestion** in the Planned backlog below, written up in full rather than left as a one-line reminder, so it can't get lost the same way twice. No code changed.
- **2026-09-05 (weather local tier: Ecowitt + Davis WeatherLink Live)** — Closes the local-console piece of the 3-tier weather picture. New `weather_station.rs` polls the configured console directly over the operator's own LAN -- unlike the scanner, this needed no Citadel involvement at all, since the device already has its own reachable IP. Both real API shapes (Ecowitt's `/get_livedata_info`, WeatherLink Live's `/v1/current_conditions`) were fetched and read before writing any code. Ecowitt's response format turned out to be genuinely inconsistent between fields (some carry a separate `unit` key, others embed the unit directly in the value string) -- handled by splitting the leading numeric prefix from whatever unit text follows, from either source, with a real test catching a real bug in that exact logic before it shipped (a value with no trailing unit text, like a bare `"6.3"` meant to pair with a separate unit field, was being wrongly rejected as unparseable). Everything normalizes to °F/mph/inHg regardless of source, matching `nws.rs`'s existing convention. Davis's rain-rate "counts" unit can't be converted to inches without knowing the specific collector's bucket size -- used the documented 0.01in/tip standard most US-market stations ship with, and said so plainly in the UI rather than presenting a number that could be silently wrong for a nonstandard collector. Migration v43: `station_profile.local_weather_brand`/`local_weather_host` (configured on the Station Identity form, same as `mesh_host`/`citadel_map_host`) and a singleton `local_weather_observation` table (same shape `space_weather` already established -- one station, one current reading, not a history). Also added a genuine fifth `Via` value (`Lan`) to `connectivity.rs` alongside the documented `internet|mesh|rf|manual` set -- confirmed first that `compute_overall`'s online/degraded/RF-only state machine only ever examines `via == "internet"` sources, so this couldn't have broken that logic either way, but mislabeling a LAN device as "mesh" would still have been a real, avoidable inaccuracy in what the operator sees. New display section on `WeatherPanel.tsx`. 8 new Rust tests, all against real captured response shapes for both brands -- 108 tests total, 103 passing, 5 live-only skipped. Clean TypeScript build, clean release build. RF-offline tier (NOAA weather radio SAME decode, satellite APT/LRPT imagery) is still open -- a substantially bigger undertaking (a transmitter dataset to source, a real Citadel-side SDR decode pipeline), not started this session.
- **2026-09-05 (real theme system: Dark/Light/Red)** — Second half of the layout/theme conversation. `App.css` had an explicit, deliberate "single theme, not a toggle" decision written into its own header comment, backed by real ham-operator feedback from initial design. Surfaced that to Frank before touching it rather than silently overriding it; he chose to build all three themes from his mockup anyway, for two real reasons: genuine night-vision preservation (field use) and a light mode for bright environments, not just preference. Audited the CSS first rather than assuming — found it was already 265 uses of `var(--...)` against a small, well-organized `:root` palette, plus exactly two real gaps fixed along the way: a dead `var(--accent, #c9a227)` fallback (`--accent` was never defined anywhere) and two hardcoded `#333`/`#2a2c26` status-dot colors that would have silently ignored theming. Restructured into `:root[data-theme="dark|light|red"]` blocks, same variable names throughout. The red theme is genuinely monochromatic, not a tint — every accent (amber/green/gold/purple/blue/gray) becomes a shade of red distinguished by brightness/saturation only, the way real red-lens field instrumentation works; mixing in another hue would defeat the entire point. Persisted via migration v42 (`station_profile.theme`, default `'dark'`) and a `set_theme` command, same "instant apply, own command, not a Station-form field" pattern already established for `tactical_mode`. New `AppearancePanel.tsx` (Settings). Found and fixed two related latent gaps while implementing: neither `set_theme` nor the pre-existing `save_station_profile` had a testable `_conn` split (the dominant pattern everywhere else in `db.rs`) or any test coverage at all -- both fixed, giving real coverage to `save_station_profile`'s carry-through logic for the first time, not just the new theme field. Verified live in a real browser: computed CSS variable values confirmed correct for all three themes (not just compiled), and clicking the Red radio in the actual rendered Settings panel flipped `data-theme` live. 4 new Rust tests -- 100 total, 95 passing, 5 live-only skipped. Clean TypeScript build, clean release build.
- **2026-09-05 (navigation restructure: retire "EmComm," add per-feature pages)** — Frank shared layout/theme mockups he'd been sketching from and asked to "split some parts into their own pages and add links to the left hand menu" — the real trigger being that "EmComm" had grown to 12 stacked panels on one scrolling page as flight tracking/scanner/weather each landed there tonight. Confirmed the direction, then executed: retired the "emcomm" `PanelCategory`, replaced with `incident-ops` (Situation Summary, Incidents, Personnel, Resource Requests, Resources, Prepare for Offline), and four new standalone pages — `tactical-map`, `flight-tracking`, `scanner`, `weather` — each substantial enough to stand alone rather than being a fragment. Net Control and Messages (ICS-213/309) moved into the existing `messaging` category alongside Winlink/JS8Call/Mesh, consistent with Frank's own "keep all comms and emergency traffic together" call from earlier the same day. Sidebar now has 12 entries instead of 8. Verified by actually running the dev server and clicking through every new tab (not just a TypeScript compile) — confirmed each shows exactly the intended panels, no console errors, and the existing invoke()-fails-gracefully-outside-Tauri behavior holds on every new page. Clean TypeScript build, clean release build. Pure frontend reorganization -- no Rust changes, no test count change. Theming (the mockups' Dark Tactical/Light/Red-low-light presets) is a separate, not-yet-started piece of the same conversation.
- **2026-09-05 (weather: NWS forecast + radar, WeatherPanel.tsx)** — Frank laid out a 3-tier weather vision (online/RF-offline/local-console) and asked for thoughts; researched each tier's real options before proposing anything (RainViewer's free no-key radar tile API, NOAA APT/LRPT satellite reception's actual requirements and current best-practice shift toward METEOR-M, which of two candidate local weather-station brands actually has a no-cloud local API — corrected an initial wrong assumption that Ambient Weather's WS-2902 did; it doesn't, only their GW1000-class gateway does). Frank picked the online tier first. `nws.rs` gained real forecast fetching — the documented two-step `api.weather.gov` flow (`/points` resolves a lat/lon to the exact forecast URL, which isn't computable any other way), migration v41's `forecast_periods` table (same replace-on-fetch pattern as `pota_spots`/`aircraft_tracks`). `TacticalMapPanel.tsx` gained a "Show Radar" toggle: fetches RainViewer's public `weather-maps.json` (free, no key, verified live before building) and adds the latest frame as a MapLibre raster layer, purely additive and removable, never blocking the base map if RainViewer's unreachable. New `WeatherPanel.tsx` (EmComm tab) is the dedicated weather page Frank asked for, showing the forecast periods; radar stays on the map since it's inherently a map layer, not a list. 2 new Rust tests (parsing a real captured NWS forecast shape, including the legitimately-null `probabilityOfPrecipitation.value` case) — 98 tests total, 93 passing, 5 live-only skipped. Clean TypeScript build, clean release build. RF/offline tier (NOAA weather radio SAME decode, satellite imagery) and local tier (Ecowitt or Davis WeatherLink Live console integration) are both scoped in the Planned backlog below, not started.
- **2026-09-05 (scanner setup + display, ScannerPanel.tsx)** — Frank asked how to make scanner/flight-tracking/weather work for anyone, not just his own install; researched the real options rather than assume (RadioReference is actually free to browse — a subscription only adds bulk CSV export/API access, not the underlying data; digitalfrequencysearch.com gives P25 frequencies straight from FCC license filings with no account at all; OpenMHz shares talkgroup files publicly). Picked scanner first. Then, mid-build, Frank redirected the UI question specifically: "will the front end be ok added to way station? id like to keep all comms and emergency traffic in waystation" — matching exactly why Citadel's `comms.html` was deleted the same day. Split accordingly: Citadel keeps only the backend (`scanner_config.py` — `build_trunk_recorder_config()`/`validate_talkgroups_csv()`, schema verified against trunk-recorder's own `CONFIGURE.md` rather than guessed at; new `GET`/`POST /api/scanner/config` routes on `vault-api`, validates both the config shape and the talkgroups CSV in full before writing either file), while WayStation gets the entire operator-facing side: `citadel_scanner.rs` (reuses `station_profile.citadel_map_host`, same configurable-host pattern the map tiles already established, talks to Citadel via `reqwest` from the Rust backend like every other external integration — no browser CORS involved) and `ScannerPanel.tsx` (EmComm tab) — one panel combining the setup form and the live status/transcripts display, not two. Verified for real at every layer: 15 Python unit tests for the config generator (`test_scanner_config.py`, stdlib `unittest`, no new dependency — this repo had no test framework before), 2 new Rust unit tests, and a real `#[ignore]`d Rust test that round-trips an actual config through the live running Citadel container (POST → confirm files land correctly on disk → GET reads them back → cleans up after itself) — not a mock, the genuine HTTP/JSON path this panel will use. 96 tests total, 91 passing by default, 5 live-only skipped. Clean TypeScript build, clean release build. Still genuinely open, hardware-blocked not code-blocked: an actual RTL-SDR dongle and Citadel's trunk-recorder→`scanner_state.json` bridge script, which can't be written against no running decoder to test it.
- **2026-09-04 (flight tracking on the Tactical Map)** — Frank confirmed the new Flight Tracking panel working live against real OpenSky data, then asked for it on the map too: "we should wire it in to the map.. it would be easyer to track flights to disaster areas." `TacticalMapPanel.tsx` now plots every aircraft with a known position (white pins, callsign/altitude/speed/on-ground status in the popup), reusing the same `get_aircraft_tracks` command and `aircraft-tracks-changed` event the standalone panel already has — no backend changes needed. Deliberately not incident-filtered, same treatment as mesh nodes/net check-ins/resources: aircraft are live external truth, not an operator-declared incident object, and the actual use case (noticing a SAR or firefighting aircraft converging on an area) often comes *before* anyone's formally declared an incident there. Clean TypeScript build, clean release build; no new tests needed since no new logic was added beyond plotting already-tested data.
- **2026-09-04 (flight tracking, ADS-B online half)** — Closes the online-first half of the operator-flagged critical "Flight tracking (ADS-B)" backlog item — flagged critical alongside weather and the scanner given a real high-disaster-risk location, and picked as today's second slice specifically because, unlike those other two, the online half needs no Citadel-side hardware work to build: OpenSky Network's `/states/all` endpoint is a free, keyless public API, confirmed live with a plain `curl` before any code was written. New `flight_tracking.rs`, migration v40 (`aircraft_tracks`, same replace-on-fetch pattern as `pota_spots`), `FlightTrackingPanel.tsx` in the EmComm tab. The station's own grid square (already collected at first-run) is reused to build a ~135-mile query box — no new configuration needed. OpenSky's state vectors are a real third-party positional array with legitimately-null fields (aircraft on the ground often report no barometric altitude); parsed defensively into `serde_json::Value` by index rather than a typed tuple that would fail the whole record on one surprise, with a real fallback to geometric altitude and trimming for OpenSky's space-padded callsigns. Explicitly not built, named rather than silently absent: the local `dump1090`/`readsb` SDR fallback for offline operation — genuinely hardware-blocked, not code-blocked, tracked the same honest way mesh multi-node testing is. 4 new tests (93 total, 89 passing, 4 live-only skipped). Clean TypeScript build, clean release build.
- **2026-09-04 (automated background sync loop)** — Closes the last open item under "Peer synchronization" from "The missing shared operational core." New `auto_sync.rs`, built on exactly what the last two sessions already shipped: `net_sync.rs`'s authenticated transport (refactored slightly -- `sync_with_peer_conn(conn, host, port)` now takes a plain `&Connection`, the established `_conn` split, so both the operator-triggered Tauri command and this new background poller can share the same connect-and-exchange logic) and `discovery.rs`'s live peer list. Migration v39 adds `trusted_peers.auto_sync`, defaulting to 0/off for every existing and newly-added peer -- the real design work here, matching the backlog item's own framing, was making sure this stays a second, explicit, per-peer decision, never implied by trust alone: a station registered as trusted (enough to click "Sync via Network" once by hand) does not get synced with automatically until an operator separately opts that specific callsign in. The poller itself (checks every 5 minutes) applies a second independent gate on top of that opt-in before ever dialing out -- a peer must also be currently present in `DiscoveryState`, not just opted in once and then possibly gone; `eligible_peers()` is a pure, directly-testable function precisely so this two-gate logic has real unit coverage without needing a live mDNS browse or socket for every case. Every attempt (success or failure, with counts or the error) is recorded in a bounded in-memory history and surfaced in `SyncPanel.tsx` -- runs unattended, but never silently; an operator can see exactly what happened and when. UI also gained a per-trusted-peer "Auto-sync when seen" toggle. 9 new tests (6 in `auto_sync::tests` covering the eligibility decision -- opted-in-but-unseen, seen-but-not-opted-in, case-insensitive callsign matching, mixed lists -- and 3 in `db::tests` covering the flag's default-off state, independent per-peer toggling, and survival across a secret rotation) -- 89 tests total, 85 passing by default, 4 live-only skipped (unchanged). Clean TypeScript build, clean release build (`localhost:1420` count still 1).
- **2026-09-03 (authenticated TCP sync transport)** — Closes the last open piece of "Peer synchronization" from "The missing shared operational core" -- a real local TCP transport, not just files. New `net_sync.rs`, built exactly the way `sync.rs`'s own module doc comment anticipated: wraps the existing transport-agnostic reconciliation functions (`export_manifest`, `uuids_needed_from`, `export_objects`, `merge_incoming`) in a network exchange, nothing about the reconciliation logic itself changed. On-demand only -- an operator clicks "Sync via Network" for one specific, already-trusted station in `SyncPanel.tsx`; true automatic background syncing stays the separately-tracked, not-yet-built "Automated background sync loop." Access control was the real design work: a connecting station gets nothing -- no manifest, no objects -- until it proves it knows a secret this station has already registered under that callsign in `trusted_peers` (an HMAC of a client-generated nonce, reusing the exact shared-secret model WSP/1 signing already established). Discovery finding a station is still not permission to sync with it, per this document's own second non-negotiable principle -- the "Sync via Network" button only even appears in the UI for a discovered peer that's also a registered trusted peer. `discovery.rs`'s advertised port is now the real listener port instead of a nominal placeholder. Caught a real protocol-sequencing bug during testing, not just a hypothetical one: the first draft of the exchange had both sides run an identical linear sequence (send request, immediately expect an answer) that deadlocks/mismatches the moment both sides run it at once, since both send their request at the same logical step before either has an answer ready -- found by the real two-socket integration test actually running two independent connections rather than assuming the sequencing was correct, fixed by reordering to read-and-serve-the-peer's-request before reading the answer to one's own. 6 new tests (`net_sync::tests` -- 4 auth accept/reject cases, a real bidirectional convergence over an actual TCP socket on loopback, and an untrusted-connection rejection) — 80 tests total, 76 passing by default, 4 live-only skipped. Clean release build.
- **2026-09-03 (mDNS peer discovery)** — Closes the "mDNS/DNS-SD local peer discovery" backlog item. New `discovery.rs`, built on `mdns-sd` (chosen specifically for having "no async runtime dependency" per its own description — same reasoning as the `meshtastic` crate, no second concurrency model for one integration). Advertises this station as `_waystation._tcp.local.` with a callsign TXT record; the advertised port is nominal, not a live listening socket, since file exchange is still the only real sync transport WayStation has. Browses for other instances and surfaces them read-only in `SyncPanel.tsx`'s new "Discovered on This Network" section. Before writing the real feature, spike-tested whether mDNS/multicast even works in this environment at all, given `meshtasticd`'s own simulated-RF multicast layer had behaved unpredictably in an earlier session — it does: a same-process advertise-then-browse test resolves in under a second, a real, non-`#[ignore]`d test (`discovery::tests`), unlike the mesh/Pat/JS8Call live tests which need external services or hardware and stay `#[ignore]`d by default. **Discovery only, never automatic sync** — this document's own second non-negotiable principle, restated in the module's own doc comment: nothing discovered is clickable to sync, moving data anywhere still always means a deliberate Export/Import. 1 new test (74 total, 70 passing by default, 4 live-only `#[ignore]`d — unchanged). Clean release build.
- **2026-09-03 (WSP/1 object signing)** — Closes the "Cleartext HMAC signing" backlog item from the same day's roadmap reconciliation. Migration v38: `station_profile.signing_secret` (this station's own HMAC key, generated once via `get_or_create_signing_secret`, never silently regenerated) and a new `trusted_peers` table (callsign → the secret *they* gave you, out-of-band, to verify things claiming to come from them). Trust model is deliberately shared-secret-per-known-station, not a PKI — adequate for a family/friends net, not an open one; documented as such in [WSP-1.md](WSP-1.md)'s new "Signing" section rather than oversold. `WspEnvelope::Objects` gained a `signature: Option<String>` field, additive via `#[serde(default)]` so a pre-signing WSP/1 file still parses cleanly as unsigned rather than failing to deserialize — no `wsp_version` bump needed. Signature is HMAC-SHA256 over `wsp_version`+`origin_callsign`+`generated_at`+`entries`, computed and checked with the `hmac`/`sha2` crates already used elsewhere in this codebase for `content_hash`, not hand-rolled. `import_objects_from_file` now returns a `SignatureStatus` (`Verified`/`Unsigned`/`UnknownSigner`/`Invalid`) on `MergeReport` — surfaced prominently in `SyncPanel.tsx`, never enforced: a merge proceeds regardless, matching this protocol's standing "flag, don't guess" rule for conflicts. `SyncPanel.tsx` also gained a Trusted Peers manager (add/remove, case-insensitive callsign lookup) and a reveal/copy control for this station's own secret, with an explicit warning never to share it over the sync channel itself. 12 new tests across `sync.rs` (signing/verifying/tampering/unknown-signer/legacy-unsigned) and `db.rs` (secret persistence, trusted-peer CRUD, case-insensitive lookup) — 73 tests total, 69 passing, 4 live-only skipped by default (unchanged). Clean release build. Explicitly not built, named in WSP-1.md rather than silently absent: secret rotation, and signing individual objects rather than the whole envelope.
- **2026-09-03 (roadmap reconciliation)** — Frank brought a longer external planning list (six categories, ~25 items) to reconcile against this document rather than assume it was accurate. Checked every item against the real codebase instead of taking the list at face value: found one item already fully done (offline map tiles — built via PMTiles since 2026-09-01, the external list assumed `.mbtiles` and called it planned), found two items already documented elsewhere but not surfaced here (WSP/2 compact encoding and packet fragmentation were both already named in [WSP-1.md](WSP-1.md)'s "What's not in WSP/1" section), and found the **"missing shared operational core" section itself had gone stale** — it still said WSP/1 was "not started," directly contradicted by the rest of this same document; fixed. New **Planned backlog** section added covering everything kept from the reconciliation, organized by area. Key decisions made along the way: (1) auto-discovery (mDNS) must never imply auto-sync — a peer being visible on the network is not permission to send it data, ever, without an explicit operator choice each time; added as this document's second non-negotiable principle, since it'll shape several backlog items' eventual design. (2) Weather/scanner/flight-tracking capture belongs on Citadel (the hardware-hub side, same role it already plays for map tiles), WayStation displays with the same online-degrades-to-local pattern already proven — flight tracking specifically flagged as operator-critical given a real high-disaster-risk location. (3) AI integration reuses Citadel's existing Ollama/Kiwix stack rather than standing up a second one, except keyword-triggered life-safety alerts, which stay deliberately non-AI (deterministic keyword matching) so they don't fail if Citadel's AI stack is down. (4) HMAC signing kept despite two adjacent ideas (Part 97 cleartext toggle, GPS-disciplined time) being dropped as low-value — signing has a real anti-spoofing safety case on an open RF path that the other two don't share. (5) A standalone-for-the-public release path is now a stated goal, documented as secondary to Citadel integration rather than competing with it — mostly already falls out of the configurable-host-with-graceful-degrade pattern already used everywhere, needs a pre-release audit rather than new engineering. Dropped: Part 97 toggle, GPS time master, canonical-header migration for `net_roster`/`resources`/`channels`. Parked (not deleted): FEMA/Red Cross shelter forms, revisit only if a standalone public release happens. No code changed in this entry — planning and documentation only.
- **2026-09-03 (mesh live verification)** — `mesh.rs` gets its first live integration tests, closing a real gap: every other transport (Pat/Winlink, JS8Call) had `#[ignore]`d tests confirming the app's own code reaches a real running service, but mesh never had, only hand-verification from months earlier. Discovered a real `meshtasticd` system service already running on this machine (not something this session started) — confirmed with Frank first that it's software-only with no real radio attached before connecting anything to it, since sending through a service that might be keying a real transmitter would be a genuinely different risk than testing against local software. `handshake_against_the_real_local_meshtasticd_yields_my_info_and_config_complete` runs the real client-api handshake (`WantConfigId` → real `MyInfo`/`NodeInfo`/`ConfigCompleteId`) through this file's own `write_to_radio`/`read_from_radio` functions against the live daemon; `sending_a_real_broadcast_text_message_does_not_error` sends a real packet that the daemon's own log confirms receiving (`[ServerAPI] Received text msg ... msg=WAYSTATION TEST...`) — not just "didn't throw," independently confirmed landing. Also investigated running a second `meshtasticd --sim` instance on a different port to prove genuine multi-node delivery (direct messages, position request/response, synchronized pins — the pieces still flagged as hardware-blocked) without needing real hardware. That didn't pan out: two separate `--sim` processes on one host turned out to be fully RF-isolated from each other with no shared virtual medium, a real architectural finding, not a config mistake to chase further — noted honestly rather than left unexplained. What's still open: this tests `mesh.rs`'s own wire-level functions directly, not the full `MeshTransport`/`dispatch.rs` send path, which is coupled to live Tauri app state (`MeshState`, `AppHandle`) and would need a fuller harness to exercise the same way. 2 new tests (62 total, 58 passing by default, 4 live-only `#[ignore]`d — 2 existing Pat/JS8Call plus these 2). Clean release build.
- **2026-09-02 (CI/packaging)** — `.github/workflows/ci.yml`: cargo check, cargo test --lib, and a real TypeScript build run on every push/PR to main, matrixed across ubuntu-22.04 and windows-latest -- the two platforms the fleet actually runs on. `.github/workflows/release.yml`: pushing a version tag (`git tag v0.2.0 && git push origin v0.2.0`) builds real installers via `tauri-action` -- `.deb`/`.AppImage` on Linux, `.msi` on Windows -- and attaches them to a **draft** GitHub release, not a published one, so a tag push can't put an unreviewed build in front of the fleet without someone deliberately opening the draft and hitting Publish. Linux bundling is scoped to `deb,appimage` explicitly rather than `tauri.conf.json`'s `"all"` target, since `.rpm` needs `rpmbuild` that isn't reliably on the hosted runner and the fleet's hardware is Debian/Ubuntu-family anyway. `ci.yml` is self-verifying -- it runs for real the moment this commit reaches `main`, and it did: first run passed clean on both platforms (8m50s Linux, 15m37s Windows cold; ~2 minutes on the second run once the Rust cache warmed up), flagging one deprecation annotation (`actions/checkout`/`actions/setup-node` pinned to Node 20 internals GitHub is phasing out) that got fixed by bumping both to their `v7` major tags, confirmed clean on the next run. `release.yml` stays genuinely unverified until an actual version tag gets pushed, which hasn't happened yet.
- **2026-09-02 (first-run onboarding)** — `FirstRunWizard.tsx` replaces the old one-line `FirstRunBanner` (deleted). The banner just said "go set your callsign in Settings" and hoped; the wizard is a real dismissible-at-any-step sequence -- Welcome, Station Identity (callsign + grid square, with the same 4-vs-6-character grid precision warning `StationIdentityPanel` already has), Citadel Integration (optional map-tile host), a one-screen note on radio/mesh hardware, and Done. No backend or schema changes -- it's built entirely on the existing `get_station_profile`/`save_station_profile` commands, carrying through every field it doesn't touch on each save so it can't silently clobber settings configured another way. "Skip for now" is available on every step and still persists whatever's already validly filled in on the current step, rather than discarding it. Shows automatically only while `callsign` is empty, same trigger the old banner used; renders as a non-blocking overlay on top of the dashboard, not a screen that has to be gotten through first, since someone might need a panel before finishing setup. The User Manual's **Start Here** page (written for someone who's never used WayStation before -- precisely who will see this wizard) got a step added explaining what the box is and how to skip past it. Clean TS build, clean release build; no backend tests needed since nothing in `db.rs` changed. **Not yet verified by an actual human click-through** -- the wizard's `invoke()` calls need a real Tauri window, so this remains an honest gap the same way Peer Sync's UI was, until someone runs it for real.
- **2026-09-02** — WSP/1 formalized: a real versioned envelope (`wsp_kind`/`wsp_version`/`origin_callsign`/`generated_at`) wraps every sync export instead of a bare JSON array, so a future format change fails clearly instead of silently misparsing. [WSP-1.md](WSP-1.md) documents what's actually built, naming what's deliberately not (compact/binary RF encoding, fragmentation, signing, TTL/forwarding, non-message/marker object types) rather than leaving it silently absent. 4 new tests — one of which failed on first write (a test bug, not a code bug: `station_profile` has no row until one's ever been saved, so an `UPDATE` in the test matched nothing) and got fixed rather than the assertion loosened. Phase C is now done except for the piece that's genuinely blocked on hardware. Phase D (Incident Operations) started same day: slice 1 — real multi-incident lifecycle (`incidents` table, create/close), `incident_id` tagging on messages/markers finally put to use, `IncidentsPanel.tsx`. Slice 2 — `personnel` table (status + incident assignment, distinct from `net_roster`'s radio-check-in concept), `PersonnelPanel.tsx`. Slice 3 — `resource_requests` (real fulfillment lifecycle, distinct from the bracket-token resources status board), `ResourceRequestsPanel.tsx`. Slice 4 — `incident_events` operational timeline (generalizes the `delivery_attempts` pattern), wired into every incident-touching write path across all three earlier slices, timeline view in `IncidentsPanel.tsx`. Slice 5 — `sitreps` table: unlike everything else built today, a SITREP is a permanent, per-incident sequence-numbered point-in-time snapshot, not a live current-state view — `generate_sitrep_body()` renders personnel/resources/timeline filtered to one incident into a plain-text body computed once at generation time, and generating one is itself logged as a timeline event. `IncidentsPanel.tsx` got a Generate/View/Copy SITREP view alongside the timeline view. Slice 6 (same day, second session) — tactical-map-by-incident: `grid_square` added to `personnel`/`resource_requests` (v37 migration, same store-square/derive-coordinates pattern the `resources` board already used, kept separate from the existing free-text `location` field), `set_person_location`/`set_resource_request_location` commands, a `GridSquareField` row control in both panels, and an incident selector on `TacticalMapPanel.tsx` that filters markers/personnel/resource-requests to the selected incident while mesh nodes/net check-ins/the generic resources board stay always-visible as non-incident infrastructure. **Phase D is now fully complete** — all 6 slices built, tested, and wired together. 60 tests total, 58 passing, 2 live-service-only skipped by default.
- **2026-09-01** — WayStation↔Citadel launch integration (`waystation://` deep link + single-instance), built and verified end-to-end. Canonical object header (v30 migration: uuid/revision/updated_at/incident_id/expires_at/trust_state on messages + map_markers). `Transport` trait, fully migrating all three real transports (mesh/Winlink/JS8Call) off the old hand-duplicated dispatch logic — a deliberate reversal of a 2026-08-29 decision to defer this exact abstraction, revisited because there were finally three real implementations to derive it from instead of designing it speculatively. Durable delivery-attempt history (v31), closing the "delivery and transport architecture" gap. Two-instance sync reconciliation (`sync.rs`) proven against two real databases — new-object convergence, stale-instance catch-up, interrupt-resume, and genuine-conflict detection all verified. `SyncPanel.tsx` UI added (Settings → Peer Sync) so this is actually clickable, not just correct in the backend — not yet human-verified through the real app. This `ROADMAP.md` created, replacing the planning-PDF as WayStation's tracked source of truth.
